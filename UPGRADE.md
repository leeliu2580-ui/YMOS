# 从 V3 升级到 V4

> V4 没有推翻 V3 的投研系统。市场洞察、投资雷达、持仓与 Watchlist、P 系列、SOP 和 Markdown 状态机继续保留。V4 做的是三件事：把投研能力扩得更长，把个人策略收拢成可审计内核，再按需接上操盘与反馈闭环。

## 先看结论

| 你担心的 | 实际情况 |
|:---|:---|
| 我改过的 P 提示词还能用吗 | **能。** 文件名和 Markdown 调用方式保持兼容，但建议补上新版归属标注 |
| 我的 SOP 定制会不会白做 | **不会。** 迁移后逐项检查它属于投研、策略内核还是操盘 |
| 持仓和 Watchlist 要重建吗 | **不用。** 从备份迁移，不要人工重录 |
| 必须先配完整 Profile 才能跑吗 | **不是。** 市场洞察、投资雷达、事实调研和 Reader 可以先跑；动作级请求在 Profile 未批准时返回 `kernel_not_ready` |
| 必须使用交易计划台和买卖决策台吗 | **不是。** 操盘层是 V4 新增能力，不是升级投研和梳理内核的前置条件 |

推荐升级顺序：

```text
保住 V3 数据
  → 在独立目录跑通 V4 投研层
  → 迁移持仓、关注、P/SOP 与定时任务
  → 把散落策略整理成 Strategy Profile
  → 接入 BrainStorm 与内核审计
  → 按需启用操盘层
```

---

## 第一步：备份，别跳过

```bash
cp -R YMOS YMOS_backup_$(date +%Y%m%d)
```

至少保住：

- `持仓与关注/`：持仓、Watchlist、个股文件夹和当前偏好。
- 你改过的 `Brain/references/p*.md` 与各模块 SOP。
- 旧投资日志、复盘记录、`BrainStorm/` 或其他长期原则文件。
- `.env`、自定义来源文件和 Agent 宿主里的定时任务配置。
- 任何你自己增加的脚本、Skills、Reader 配置和页面修改。

**把 V4 clone 到独立目录，不要用 ZIP 直接覆盖旧 vault。**

```bash
git clone https://github.com/Evan-XYZ/YMOS.git YMOS_v4
```

旧目录是回滚源，新目录是迁移目标。完成验收前，不要让两个目录同时写入真实数据。

---

## 第二步：先验证 V4 投研层

不要一上来迁移全部内核。先在干净 V4 目录验证“来时路”仍然能跑：

```text
跑一下市场洞察
跑一下投资雷达
```

然后启动 Reader：

```bash
cd Console
cp config.example.json config.json
python3 server.py
```

打开 <http://localhost:5273/reader>，确认报告能读取、链接能跳转。这个阶段只使用 Reader 即可，不要求创建交易计划或决策记录。

如果最小闭环失败，先修路径、Key 或数据源，不要继续迁移真实持仓。

---

## 第三步：迁移 V3 的投研资产

| V3 资产 | V4 放在哪里 | 怎么处理 |
|:---|:---|:---|
| `Eyes/` 自定义脚本与 SOP | `Eyes/`（投研层） | 逐个迁移，不整目录覆盖；保留 V4 的失败降级和新路径解析 |
| `持仓_状态机.md`、`Watchlist_状态机.md` | `持仓与关注/` | 从备份迁移后核对标的数量和身份 |
| `持仓/`、`动态Watchlist/` 个股目录 | 同名目录 | 原样迁移研究资料、备忘录和历史报告 |
| 改过的 P 系列 | `Brain/references/` | 保留你的逻辑，与 V4 同名文件逐项对照合并 |
| 改过的路由 SOP | `Brain/` / `Eyes/` | 保留自定义分支，同时补齐 Profile 与 Human 边界 |
| 定时任务 | Agent 宿主 + `Agents/SCHEDULES_REFERENCE.md` | 不自动注册；按 V4 路径和成功标准重建 |

### P 系列迁移时检查两件事

第一，每个模块应标明性质：

| 标记 | 含义 |
|:---|:---|
| `invariant` | 所有策略都需要的结构约束 |
| `profile` | 会随个人策略变化的判断轴 |
| `optional` | 与你的策略无关时可以停用 |

第二，把能统一管理的参数从 P 文件移到 Strategy Profile，例如仓位、止损、期限、收益分级和信息有效期。旧 P 里写死的参数不会因此失效，但继续散着维护会让 Agent 不知道哪个才是真相源。

---

## 第四步：把散落的策略整理成内核

这是 V3 → V4 真正需要动脑的部分，但**不要求先启用操盘层**。

把这些材料交给 Agent：

- `当前关注方向与投资偏好.md`
- 你改过的 P 系列和 SOP
- 旧投资日志、复盘记录和长期原则
- BrainStorm Raw / Insight / MEMORY
- 真实使用过的报告样本与定时任务

V4 的唯一运行文件是 `Brain/策略配置/当前策略_Profile.md`；`持仓与关注/当前关注方向与投资偏好.md` 只作为 V3 兼容投影。不要保留多个无法判断优先级的“当前 Profile”。

然后运行：

```text
帮我做一次投资诊断
配置一下我的内核
```

Agent 应先发现矛盾，再起草 Profile，不得从作者示例或模型常识补参数。重点确认：

1. 能力圈和明确不做的范围。
2. 入场依据与可观察的失效信号。
3. 仓位、风险预算、资金期限与论点期限。
4. 加仓、持有、退出各自由谁裁决。
5. 六类策略结构混合时，冲突由谁优先。
6. 哪些 P / SOP / 扩展启用、停用或替换。

写不出来就留空。空白意味着“尚未想清”，比抄一个看似专业的数字更安全。

只有 Profile 同时满足 `status: active`、`meta.approvedByHuman: true`、非空 scope、必要风险字段和完整 `modules` 清单，动作路由才继续。P 模块必须服从 `enabled / disabled / replacements`；缺参数返回 `data_incomplete`。

### BrainStorm 怎么迁移

BrainStorm 和旧日志不是低价值附件，而是理解你策略来路的重要语料。它们可以帮助 Agent 找到反复出现的信念、困惑和行为矛盾。

但历史感悟不应直接成为 `active` 规则。候选规则仍需写明证据、反证条件、Human 审批与回滚方式。

---

## 第五步：重建日常调度

定时任务是投研系统持续运行的触发器。先恢复两条主链：

| 任务 | 顺序 | 验收 |
|:---|:---|:---|
| 市场洞察 | 先运行 | 生成有日期、来源和失败说明的报告 |
| 投资雷达 | 市场洞察之后 | 正确读取持仓、关注和最新市场报告 |

再按需要恢复策略分析、What's Hot 或个人扩展。不要一次把旧任务全部打开；每条任务先手动跑一次，再进入调度。

提示词至少写清：工作目录、角色文件、SOP 路径、上游产物、成功标准和失败处理。参考 [`Agents/SCHEDULES_REFERENCE.md`](Agents/SCHEDULES_REFERENCE.md)。

---

## 第六步：按需启用操盘层

你的 Profile 相对清楚之后，再决定是否使用交易计划台和买卖决策台：

```bash
cd Console
cp rules.example.json rules.json
python3 server.py
```

作者提供的是页面和数据契约样板，不是通用交易答案。根据自己的习惯修改：

- 盘前计划字段和盘中动作顺序。
- 开仓、加仓、止盈、止损的确认问题。
- 仓位与资金仪表展示。
- 页面布局、提醒方式和报告入口。

V4 中“建仓”暗号只建立候选计划；只有 Console `fill` 确认真实成交后，Portfolio State 才能新增持仓。当前“加仓”入口保存的是计划与压力测试，不会改变股数、成本或实际投入。

不能修改的边界：真实成交必须由 Human 确认；事件历史只追加；`rules.json` 不能与 Human 已确认的 Profile 形成第二套策略。

如果你已经使用过早期 V4 的操盘文件，再迁移 `Brain/买入卖出决策/`、`Brain/交易计划/` 和 `Brain/决策审计/`，并用“重读文件”核对股数、成本、币种和退出规则。

---

## 第七步：接上进化闭环

V4 有两条互补路径：

```text
旧日志 / BrainStorm → 梳理信念与矛盾 → 候选规则
真实计划 / 门禁 / 平仓 → 验证判断与执行 → 结构性证据
```

启用操盘层后，每笔平仓运行 `复盘一下 [ticker]` 固定计划与实际；建议积累至少 10 笔后，再定期运行 `审计一下我的内核` 形成变更提案。

这里的 10 笔门槛约束的是**根据交易结果修改规则**。如果迁移旧日志或 BrainStorm 时已经发现 Profile / P / SOP 存在明确歧义、互相冲突或两个真相源，可以先起草规则澄清型提案并标记 `trial`；它不能冒充结果验证。

单笔结果不改内核；没有反证、审批和回滚条件的提案不进入稳定规则。

---

## 升级验收

- [ ] 市场洞察和投资雷达在 V4 目录独立跑通。
- [ ] Reader 能读取报告，页面互链正确。
- [ ] 持仓、Watchlist 和个股资料数量与备份一致。
- [ ] 自定义 P / SOP 已逐项合并，没有被新版样例覆盖。
- [ ] Profile 没有从作者案例自动复制数值，空白仍保持空白。
- [ ] 唯一运行 Profile 位于 `Brain/策略配置/当前策略_Profile.md`；动作级请求在 draft 状态被正确拦截。
- [ ] 停用模块没有被读取，替换模块没有与原模块同时运行，缺参数时返回 `data_incomplete`。
- [ ] BrainStorm 和旧日志已作为上下文保留，但未自动变成规则。
- [ ] 规则澄清型与结果验证型提案已区分；BrainStorm 没有被当成结果证据，10 笔门槛也没有阻止修复明确冲突。
- [ ] 定时任务已逐条手动验证，工作目录和成功标准正确。
- [ ] 若启用操盘层，账户、持仓、成本、币种与退出规则已核对。
- [ ] 候选建仓不会提前写入 Active 持仓；平仓后股数与实际投入归零，审计同时保留拦截与放行证据。
- [ ] Agent 不自动成交、不伪造“已执行”、不静默修改 Profile。

## 回滚

发现数据不一致，就立即停止在 V4 目录继续写入，记录最后一次成功事件并回到完整备份。

**不要把新旧两个目录同时当作真相源。**

---

*YMOS V4 · V3 升级指南 · 先保住投研主链，再装策略内核，最后按需接操盘层*
