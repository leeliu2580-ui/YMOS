# 财经自媒体 + 投研自动化系统 · 执行计划

> 本方案基于你与多个 AI 模型的深度讨论，融合了"务实版架构"、"CTO 审计修正"以及"杠杆战略分析"的核心结论。
> **目标**：在 7 天内，用最小成本跑通"热点 → 选题 → AI 报告/文案 → 飞书推送"的完整闭环，并形成可迭代的 Prompt 资产。

---

## 一、核心结论（一句话）

**当前最该 All in 的一件事：**
**用 MiniMax 2.7 API + 一个极简的 Python 脚本，手动跑完一次"选题 → 生成报告 → 人工修订 → 保存"的完整流程，并把这一过程固化为可复用的 Prompt 模板。**
其他所有工作（ChromaDB、定时任务、飞书卡片、YMOS 深度配置）都是次要的，暂时跳过。

---

## 二、系统终局定义

- **终局**：每天早晨，飞书自动推送一份**可直接使用**的 A 股投研日报（或自媒体选题+文案初稿），人工干预不超过 15 分钟。
- **不可逆的标志**：连续 3 天，你不再手动搜热点、不再从零写报告，而是直接使用 AI 产出。

---

## 三、必要前提（杠杆点）

| 前提 | 性质 | 当前状态 |
|------|------|----------|
| ① 输出质量稳定（修改率 <20%） | 必要 | ❌ 未达标 |
| ② 格式直接适配飞书/公众号 | 必要 | ⚠️ 部分（需 Markdown） |
| ③ 系统能隔夜自动运行 | 必要 | ✅ 已有云服务器 + cron |
| ④ 选题方向命中你关心的领域 | 必要 | ⚠️ 需验证 |
| ⑤ 人工干预成本低于手动 | 分界点 | ❌ 未达标 |

**杠杆点就是第 ① 条**。一旦输出质量达标，整个系统就会被动嵌入你的工作流。

---

## 四、六层深挖（理解为什么必须从 Prompt 开始）

| 层次 | 内容 | 为什么很多人停在这里 |
|------|------|----------------------|
| 第1层 | 配 API、写脚本、调定时任务 | 做完有"完成感"，但实际没产出 |
| 第2层 | 跑通 API 调用，能返回文本 | 以为"通了=成功"，不管内容质量 |
| 第3层 | 抱怨模型不够好、换更贵的模型 | 不知道可以通过 Prompt 注入分析框架 |
| 第4层 | 写一个长长的 Prompt，但每次随缘 | 没有系统化迭代，结果不可控 |
| 第5层 | 开始记录 Prompt 版本和效果 | 开始积累核心资产 |
| 第6层 | **拥有一个经过 10+ 次迭代、能稳定输出 70 分报告的 Master Prompt** | 到达这一层，胜局开始 |

**你现在要做的，就是直接跳到第 5、6 层**——跳过前 4 层的弯路。

---

## 五、7 天执行计划（精确到天）

### Day 1（今天）：跑通第一个"人工辅助闭环"
- **动作**：
 1. 手工选择一个今天的 A 股热点（例如"宁德时代财报"或"央行降准"）。
 2. 用你的笔记本，打开飞书或任何编辑器，写一段**给 MiniMax 的输入**，包含：
 - 选题标题
 - 你已知的背景（2-3 句）
 - 你希望报告包含哪些部分（例如：事件解读、影响分析、风险提示）
 3. 通过你已经配置好的 OpenClaw（或直接调用 One-API），向 MiniMax 2.7 发送这个输入，生成第一份报告。
 4. **不管质量多差**，人工修改到你觉得"可以发朋友圈"的水平。
 5. 记录你修改了哪些地方（比如：数据错了、逻辑跳跃、废话太多）。
- **产出**：第一篇完整的、可发布的报告（哪怕很短），以及**第一次修改记录**。

### Day 2：提炼第一个 Prompt 模板
- **动作**：
 1. 基于昨天的输入和修改记录，写出第一个**结构化 Prompt**，至少包含：
 - **角色**："你是一位 A 股资深分析师，擅长用数据说话，语言简洁。"
 - **任务**：按照"背景 → 核心影响 → 受益/受损板块 → 风险提示"的结构输出。
 - **格式要求**：Markdown 格式，直接可复制到飞书。
 2. 用同一个热点（或换一个热点）测试这个 Prompt，生成第二份报告。
 3. 再次人工修改，对比修改量是否减少。
- **产出**：`prompt_v1.md` 文件，保存在 `memory/prompts/` 目录。

### Day 3：加入 Few-Shot 和 CoT
- **动作**：
 1. 在 Prompt 中加入一个 **Few-Shot 示例**（把昨天你觉得最好的一段分析贴进去）。
 2. 加入 **Chain of Thought（思维链）** 指令，例如：
 > 请按以下步骤思考：
 > 第一步：判断该事件是短期情绪还是长期趋势。
 > 第二步：找出最相关的 3 个行业。
 > 第三步：每个行业选 1 只代表性股票，给出逻辑。
 > 第四步：总结风险。
 3. 测试 2-3 个不同热点，记录每次输出的质量（1-5 分）和修改耗时。
- **产出**：`prompt_v2.md`，并形成初步的评分标准。

### Day 4：增加"自检环节"
- **动作**：
 1. 要求 MiniMax 在输出报告后，**再输出一段"自检"**：
 > 请检查上述报告是否有以下问题：
 > - 数据是否过期？
 > - 逻辑是否存在跳跃？
 > - 是否存在模糊表达如"可能""大概"？
 > 如有，请重新修改。
 2. 对比有无自检环节的质量差异。
- **产出**：`prompt_v3.md`，自检版本。

### Day 5：封装成脚本，支持命令行调用
- **动作**：
 1. 写一个极短的 Python 脚本（< 50 行），功能：
 - 从命令行接收一个选题（或从文件读取）
 - 拼接 Prompt
 - 调用 One-API（MiniMax 2.7）
 - 将返回结果保存到 `output/` 目录，并打印到控制台
 2. 测试 3 个不同选题，确保稳定运行。
- **产出**：`generate_report.py`。

### Day 6：接入飞书，实现"手工触发"
- **动作**：
 1. 将上面的脚本包装成一个简单的 HTTP 服务（或用 OpenClaw 的飞书技能）。
 2. 在飞书群里发送"报告 宁德时代"，触发脚本，返回报告内容。
 3. 测试成功即可，不用做复杂的按钮交互。
- **产出**：飞书机器人能响应指令并返回报告。

### Day 7：配置第一个定时任务（可选）
- **动作**：
 1. 在腾讯云服务器上，写一个 cron 任务：
 ```bash
 30 8 * * * cd /home/YMOS && python generate_report.py --topic "今日热点" --output /home/reports/$(date +\%Y\%m\%d).md
 ```
 2. 配合 Server酱 或飞书机器人，将生成的文件推送到你的手机。
- **产出**：自动日报雏形。

**如果时间紧张，Day 6 和 Day 7 可以推迟到第二周**。前 5 天必须完成。

---

## 六、必须遵守的原则

| 原则 | 说明 |
|------|------|
| **极简优先** | 不用数据库、不用 ChromaDB、不用本地模型、不用 Docker 编排。只用 Python + API + 文件。 |
| **人在回路** | 所有 AI 输出必须经你人工确认后才能发布。 |
| **可观测** | 每次生成都记录：选题、Prompt 版本、修改量、耗时。 |
| **版本化** | Prompt 像代码一样保存，每次修改备注原因。 |
| **先闭环，再优化** | 哪怕输出只有 40 分，也要跑完整条链路。 |

---

## 七、禁止事项（红牌）

- ❌ 不要花时间研究 ChromaDB 或向量数据库
- ❌ 不要尝试在笔记本或云服务器上运行本地模型
- ❌ 不要写超过 200 行的 Python 脚本
- ❌ 不要做飞书交互卡片、按钮、菜单
- ❌ 不要试图一次性优化定时任务到毫秒级
- ❌ 不要因为一次生成不好就放弃，迭代 Prompt 至少 5 次

---

## 八、成功标志（可量化）

- [ ] 第 3 天结束，你有一份 `prompt_v2.md`，并且用它生成的报告修改量小于 30%。
- [ ] 第 5 天结束，你能在 5 分钟内用脚本生成一份报告。
- [ ] 第 7 天结束，你收到了一条来自云服务器的自动推送，内容是一份可读的日报。

---

## 九、风险应对

| 风险 | 应对 |
|------|------|
| MiniMax 生成内容数据错误 | 在 Prompt 中强制要求"不确定的数据请标注'需核实'"，并人工快速检查 |
| 飞书推送失败 | 先用本地文件验证，推送是锦上添花 |
| 脚本卡住或 API 超时 | 设置 timeout 60 秒，失败后重试一次 |
| 自己坚持不下去 | 每天只要求自己跑通一个选题，完成后立即奖励自己 |

---

## 十、Prompt 资产目录结构

```
memory/
  prompts/
    # 模板
    prompt_v1.md    # Day2 产出，基础结构化模板
    prompt_v2.md    # Day3 产出，Few-Shot + CoT
    prompt_v3.md    # Day4 产出，自检环节
    master_prompt.md # 迭代成熟后合并
    # 记录
    generation_log.md  # 每次生成记录（选题/版本/修改量/评分）
  sessions/
    # 每次跑闭环的 session 记录
output/
  reports/           # 生成的报告存档
  drafts/            # 待修订版本
```

---

## 十一、Git 提交规范

> 每次修改 Prompt 或脚本，必须通过 Git 提交并做好记录。

### 提交信息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 规范
| Type | 使用场景 |
|------|---------|
| feat | 新增 Prompt 模板或功能 |
| fix | 修复 Prompt 逻辑问题 |
| docs | 文档更新（如本文件） |
| refactor | Prompt 重构（结构优化） |
| test | 测试验证记录 |
| chore | 辅助脚本或配置更新 |

### 示例
```bash
git add memory/prompts/prompt_v2.md
git commit -m "feat(prompt): add Few-Shot and CoT to v2

- Add one high-quality example from Day1 report
- Add 4-step thinking chain
- Test with 3 topics, avg score 3.8/5

Closes #3"
```

---

*文档版本：v1.0.0*
*创建时间：2026-04-21*
*下次审查：2026-04-28（第一周迭代节点）*