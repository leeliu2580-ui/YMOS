# HEARTBEAT.md

## Backup Check

每天执行：
- Windows: `./backup.ps1`
- Git Bash / Linux / macOS: `./backup.sh`

检查规则：
- 如果 push 成功：`BACKUP_OK`
- 如果 push 失败：`BACKUP_FAILED`

说明：
- 优先使用当前环境可执行的脚本
- 如果没有变更，允许跳过 commit，但仍要检查 push 是否可用

---

## 待办系统检查

每次心跳检查 `TODO.md`：

### [A] 待确认项
- 发现 `[A]` 项 → 在对话中向用户发起确认请求
- 格式：`您有一个待确认任务：[任务名]，原因：[备注]，是否执行？`

### [P] 待执行项
- 发现 `[P]` 项 → 判断是否可以执行
- 可执行 → 执行 → 标记为 `[D]` + 写入当日 memory 日志
- 不可执行 → 保持 → 下次心跳再检查

### [D] 已完成项
- 首次发现某 `[D]` 项 → 写入当日 `memory/YYYY-MM-DD.md` 的「今日完成」章节
- 已在当日日志中出现过的 `[D]` 项 → 跳过

---

## Memory Maintenance

**每3天执行一次**（检查 `TODO.md` 中的到期日）：

触发条件：
- `TODO.md` 中有 `[P]` 项到期（当前日期 >= 到期日）
- 或距上次执行已超过3天

执行动作：
1. 读取 `memory/` 下近3天的日志
2. 提炼值得长期记住的内容
3. 更新 `MEMORY.md`
4. 将该 `[P]` 项标记为 `[D]` + 写入当日 memory 日志
5. 在对话中汇报结果

**提醒规则**：
- 到期时在心跳中询问用户是否执行（`[A]` 状态）
- 用户确认后 → 降级为 `[P]` → 执行

