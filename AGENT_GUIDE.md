# YMOS Agent Guide

> Agent 每次新会话先读本文件。YMOS 是 Human-in-the-loop 系统，不授权 Agent 自动交易或伪造执行记录。

## 1. 三层定位

| 层 | Agent 的工作 | 禁止 |
|:---|:---|:---|
| 投研层 | 获取事实、做初始研究、记录来源与失败 | 把数据形态直接写成个性化交易结论 |
| 策略内核层 | 读取 Profile、分析、发现矛盾、提出建议 | 替用户设置参数或静默修改稳定内核 |
| 操盘层 | 读取并协助生成计划、门禁与审计 Markdown | 代替用户生成已执行、已成交证据 |

归层规则见 `ARCHITECTURE.md`。

## 2. 接到配置或升级请求时：先路由到进阶指南

`进阶指南.md` 是用户侧的改造说明，本文件是 Agent 的实施协议。用户说“帮我配置”“帮我升级”“按我的习惯改”时，不能只按本文件的权限表机械写文件；必须先完整读取 `进阶指南.md`，判断需求属于哪一节，再实施。

### 需求路由表

| 用户想做什么 | 先读进阶指南 | Agent 的主要动作 |
|:---|:---|:---|
| 配 RSS、Finnhub、Tushare、Market API 或其他数据源 | §1.1–1.4 | 盘点市场与数据缺口；创建/更新 `.env`；测试数据、时间、失败降级 |
| 安装问财或其他 Skill | §1.1 的问财部分、§5 | 使用官方安装说明；选择宿主标准目录；配置 `YMOS_SKILL_ROOT`；同时验收宿主注册和 CLI 发现 |
| 增加投研脚本、报告或定时任务 | §1.2–1.5、§6.3 | 定义原子输入输出、来源时间、错误结构、SOP 依赖和调度顺序 |
| 梳理或升级投资逻辑 | §2、§4 | 先诊断和读取旧日志 / BrainStorm；起草 Profile 或变更提案；参数留给 Human |
| 修改交易计划、门禁或生命周期 | §3、§6.5 | 从 Profile 投影规则；确认 Human 节点；保持事件只追加；验证 Console 路由和写盘 |
| 修改 Reader、页面或看板 | §3.4、§6.5 | 区分阅读偏好和策略规则；修改 config / HTML；不得让页面成为第二套内核 |
| 选择 Agent、模型或多角色协作 | §5 | 检查文件、Python、Web、Skill、调度与记忆能力；一个统筹，多执行工具共享文件真相源 |
| 按个人参数整体升级三层 | §6 | 先整理参数包；分别形成投研、策略、操盘/看板改造清单；分层实施和验收 |

### 标准实施流程

```text
理解目标
  → 读取进阶指南对应章节与当前真相源
  → 盘点现状、已知参数、空白和冲突
  → 向 Human 展示分层方案与需要确认的选择
  → 做最小改动
  → 验证正常、空结果、失败和权限边界
  → 把结果与下一步写回约定文档
```

能从仓库、配置或运行结果里查到的，不要反复问用户。会改变策略、资金、外部账户、付费服务或真实运行状态的选择，必须由 Human 确认。

### 2.1 配置投研层

1. 先确认用户研究的市场、时区、持仓/Watchlist 范围、信息频率和成本偏好。
2. 先跑 Level 0；只为明确的数据缺口增加 Key、API 或 Skill，不追求“把所有 Level 配齐”。
3. 读取 `.env.example`，保留现有 `.env` 中不属于本次任务的字段；只添加需要的键。
4. 数据脚本只能输出事实、来源、时间、未解决项和错误，不能生成个人买卖结论。
5. 新数据源先手动 smoke test，再接 SOP，最后才进入定时任务。
6. 验证移除 Key 后能明确降级；不能因可选接口失败阻塞基础主链。

### 2.2 配置策略内核层

1. 读取已确认 Profile、投资偏好、旧 P/SOP、诊断报告、旧日志和相关 BrainStorm。历史资料较多时先执行 `BrainStorm/SOP_历史投资资料入职.md`，读取索引和阶段梳理，不全量装载原文。
2. 区分用户明确规则、实际行为证据、候选认知与作者示例；不得把示例补进空白。
3. 区分旧日志中的当时观点、事后解释和当前仍认可规则；矛盾时展示来源并向 Human 追问，不替用户决定。
4. 先让 Human 确认能力圈、策略结构、入场依据、失效信号、期限、风险预算和冲突裁判。
5. 再把 Profile 映射到 P 系列、策略 SOP 和模块启停清单；一次只改一个可验证问题。
6. 明确歧义或冲突可以起草**规则澄清型**提案并标记 `trial`；声称规则更有效必须走**结果验证型**提案和跨笔证据。
7. 任何稳定内核修改都要有反证、复核、Human 审批和回滚。

### 2.3 配置操盘层与看板

1. 先读取 Profile、`Console/TRADE_DATA_CONTRACT.md`、Console README 和对应 SOP。
2. 从用户真实行为弱点反推门禁，不从作者页面复制手数、金额或固定比例。
3. `rules.json` 只能是 Profile 的投影；冲突时回到 Human 确认的 Profile。
4. Reader 配置解决“看什么”；交易计划台和买卖决策台解决“怎么计划、确认与留痕”。显示偏好不能创造策略规则。
5. 新生命周期动作必须先定义事件契约与服务端校验，再改页面；真实事件 append-only。
6. 修改后至少验证 `/reader`、`/plan`、`/sop`、直接 HTML 预览、API 写盘和路径越界拒绝。

### 2.4 Key、Secret 与外部 Skill

- 优先让 Human 在本地 `.env` 或宿主 Secret 管理界面填写真实 Key；Agent 不在回复、日志和测试输出中回显。
- 不把 Key 放进命令参数、Markdown、SOP、脚本常量、定时任务提示词或 Git。
- 不覆盖已有 `.env`；修改前只检查键是否存在，不打印值。完成后确认 `.env` 被 Git 忽略。
- 安装外部 Skill 时使用用户提供或官方页面生成的安装 Prompt，不猜下载地址，不复制第三方改版。
- 优先使用当前宿主的标准 Skills 目录；放在其他位置时，`YMOS_SKILL_ROOT` 指向包含各 Skill 文件夹的上级目录。
- “文件夹存在”不等于安装完成。必须同时检查：宿主能发现 Skill、`SKILL.md` 存在、所需 `scripts/cli.py` 存在、`skill_resolver.py` 能解析、最小查询成功或给出明确缺 Key 状态。

### 2.5 开发新能力

收到“增加功能”时，按下面的落地链判断修改范围：

```text
脚本 / API / Skill → SOP → Agent 角色 → Profile / 状态机 → Reader / Console → 审计
```

- 先运行 `git status`，保留用户和其他协作者已有改动，不 reset、不覆盖。
- 优先修改现有扩展点，不把数据、策略、界面和调度全部塞进一个脚本或 Prompt。
- 新配置写样例；真实运行文件加入 `.gitignore`。
- 新能力必须有输入、输出、失败降级、权限边界、最小测试和文档入口。
- 除非用户明确授权，不 commit、不 push、不发布、不改外部文档站。

## 3. 新会话读取顺序

1. `总入口暗号.md`
2. `Brain/策略配置/当前策略_Profile.md`（若不存在，读取兼容投影并视为 `draft`）
3. `持仓与关注/当前关注方向与投资偏好.md`（兼容投影，不覆盖当前 Profile）
4. `Brain/买入卖出决策/买卖决策_状态机.md`（若存在）
5. `持仓与关注/持仓_状态机.md`
6. `持仓与关注/Watchlist_状态机.md`
7. 当前任务对应 SOP 和 P 模块

如果 Strategy Profile 为 `draft`，市场洞察、投资雷达、L0 状态读取和基础事实调研照常运行；不得输出个性化动作建议。动作级请求返回 `kernel_not_ready` 时必须把它解释为可恢复的配置状态，并询问用户是否进入 `Brain/内核审计/SOP_内核入职配置.md` 的快速模式；不得只抛状态码。

### 新用户与 V3 用户的入职分流

- 第一次接触 YMOS：先完成投研层最小闭环和 Reader 验证，只写市场范围、能力圈与最小偏好；不要强迫 Day 0 填满 Profile 或启用操盘台。
- 新用户要建立策略时：一次只问一组人类问题，不直接展示整份 JSON Schema；先形成 `draft`，再单独询问是否激活。用户回答“不确定”时保留空白，不循环追问。
- 从 V3 升级：先保留并回归市场洞察、投资雷达、持仓、Watchlist、自定义 P/SOP 和定时任务，再把散落规则整理成 Profile。
- 策略内核可以通过诊断、旧投资日志和 BrainStorm 独立梳理；操盘层不是前置条件。
- 只有用户明确需要计划、门禁和真实留痕时，才引导启用操盘层。

## 4. 真相源优先级

| 事实 | 真相源 |
|:---|:---|
| 策略与偏好 | 已获 Human 确认的 Strategy Profile |
| 当前股数、成本、交易状态 | `Brain/买入卖出决策/` 单笔生命周期文件 |
| 最近价格与账户总览 | `买卖决策_状态机.md` 的 `portfolioSnapshot` |
| 持仓/关注身份与研究位置 | `持仓_状态机.md`、`Watchlist_状态机.md` |
| 盘前判断和盘中实际 | `Brain/交易计划/` |
| 放行与拦截 | `Brain/决策审计/` |
| 历史交易事件 | 单笔生命周期和 `已平仓/` |
| 已提取的执行证据 | `Brain/内核审计/优化建议/` 的平仓复盘与周期审计 |
| 候选认知语料 | BrainStorm 与用户旧投资日志；是梳理策略的重要上下文，未审批前不是稳定规则 |

快照与单笔文件冲突时，以单笔文件为事实源并要求刷新快照。

## 5. 权限

### 允许读取

- Eyes、Brain、Agents、BrainStorm 的 SOP 和参考文件。
- Strategy Profile、状态机、个股知识库和运行报告。
- Console 产生的计划、决策与审计 Markdown。

### 可以在正常流程中写入

- Eyes/Brain 的报告目录。
- 用户确认后的个股知识库和身份状态。
- BrainStorm Raw/Insight 草案。
- 用户确认后的 Strategy Profile 与判断层变更提案状态。

### 必须由 Human 操作产生

- 真实账户资金变动。
- 买入、卖出、加仓、止盈、止损和成交确认。
- “已经执行”的交易计划记录。
- 规则变更的最终审批。

Agent 不得为了让系统看起来闭环而补写这些证据。

## 6. Strategy Profile 规则

- 不从作者案例中复制数值。
- 不创建或激活所谓“通用策略 Profile”；只允许预置 `draft`、Human 门禁、决策执行分离等结构不变量。
- 不因用户自称某个流派就忽略实际行为证据。
- 发现冲突时展示选择，不替用户拍板。
- 未填字段保持 `null` 或空白，不发明默认。
- `Console/rules.json` 是 Profile 在操盘层的投影；二者冲突时以 Human 确认的 Profile 为准。
- 调用 P 模块前必须检查 `modules.enabled / disabled / replacements`：停用项不读，替换项改读替代模块，未启用的 `profile` / `optional` 模块不得按公共默认运行。
- P 模块缺少参数时返回 `data_incomplete`；不得使用文件内示例数值、作者参数或模型常识补齐。

## 7. 状态驱动分析

日常先读 `portfolioSnapshot.accounts / positions / health / agentRouting`：

- L0：无告警，只输出简报。
- L1：只打开告警对应的单笔文件。
- L2：用户明确要求或出现组合异常时才跑完整再平衡。

不要每天装载所有单笔文件，也不要在无告警时重跑全部 P 系列。

日常持仓分析默认不读取 `已平仓/`；只有交易复盘、P11 或周期内核审计才读取历史归档。

## 8. 内核变更

### 反馈闭环

```text
BrainStorm / 历史日志 ─→ 认知、矛盾与候选规则 ───────────┐
                                                        ├→ 进化审查 → 变更提案 → Human 审批
买卖决策事件流 ─→ 平仓后逐笔提取 → 跨笔聚合找重复 ──────┘                         ↓
                                                                     策略内核更新与复核
```

BrainStorm 记录“怎么想”，买卖决策与操盘记录证明“实际怎么做、结果怎样”。前者适合梳理和发现冲突，后者适合结果验证；不得让任何一边自动改内核。

每笔平仓后跑 `Brain/内核审计/SOP_平仓复盘归档.md`，把「计划 vs 实际」写入 `优化建议/YYYY-MM/`。三项与盈亏无关的红旗必须机械检出并记录，**赚钱的违规也照记**：

- 放宽止损（`adjust` 事件里新止损离成本更远）
- 亏损加仓（`add` 事件时价格低于当时成本）
- 绕过门禁（决策审计有拦截但仍然成交）

单笔复盘**不修改任何内核文件**。积累到重复出现才由周期审计转成变更提案。

### 分类

发现重复错误时先分类：

1. 策略按设计运行但结果不利。
2. 执行偏离。
3. 内核规则模糊或冲突。
4. 投研数据问题。

对应处理：

- 1：记录，不因单笔结果改规则。
- 2：优先提出执行门禁。
- 3：生成判断层变更提案。
- 4：修复驱动和数据质量。

任何内核修改都必须包含反证、复核条件和回滚方案，并由 Human 批准。

## 9. 调度

定时任务是投研层持续运行的正常触发机制。公开版规定依赖并提供参考时间表，但不替用户自动注册任务：

```text
事实就绪 → 雷达映射 → 策略/状态分析 → Human 审阅 → 盘前计划 → 盘中执行
```

第一次配置至少建议市场洞察 → 投资雷达两条主链。用户根据市场、时区、Profile 和信息频率设置具体时间；Agent 不得自动启用所有模块。每条任务进入调度前必须先手动运行一次，并明确工作目录、角色、SOP、成功标准和失败降级。

“任务名里带 Agent”不代表运行协议已经生效。每个任务必须按顺序读取：

```text
Agents/<role>-agent.md
  → Agents/EXECUTION_PLAYBOOK.md 对应段落
  → 本次具体 SOP
  → 上游产物与成功条件
```

Portfolio State 必须按触发类型分流：账户体检读 `SOP_持仓日常体检.md`，标的身份变动读 `SOP_标的管理.md`，真实成交与资金事件只能由 Human 通过 Console 按数据契约确认。不得引用已退出 V4 的 `SOP_持仓收口.md`、`持仓备忘录_视图.md` 或旧 dashboard 路径。完整映射见 `Agents/ORCHESTRATION.md`。

## 10. 失败处理

- 数据源失败：记录来源、错误和降级，不静默吞掉。
- Profile 缺失：基础事实流程继续，动作分析降级。
- 状态机缺失：提示通过本地 Console 初始化。
- 快照过期：不能把旧价格写成实时结论。
- 输出路径不存在：只在 SOP 允许的目录内创建。
- Human 未确认：保留草案或建议，不写真实事实。

## 11. 完成标准

一次任务完成前检查：

- 输入和来源写清。
- Profile 和事实没有混淆。
- 失败降级可见。
- 产物落到约定路径。
- 不越过 Human 边界。
- 如果修改内核，已有提案、反证与审批。
- 如果配置 Key / Skill，已验证发现、最小调用和无凭据降级，且没有回显 Secret。
- 如果改三层或看板，已按 `进阶指南.md` 对应章节检查上下游、配置样例和测试。
- 如果开发新能力，文档、失败降级、权限边界与最小测试已同步。

---

*YMOS V4 · Agent Guide*
