# YMOS V4 架构

> 业务上分为投研层、策略内核层、操盘层；技术上由跨层 Agents 协议把它们编排起来。

YMOS 是一套 Human-in-the-loop 的投资操作系统。它不替用户交易；它把外部信息、个人判断、执行门禁和历史证据组织成一条可审计、可复核、可进化的链路。

## 1. 三层是用户的进阶主线

| 层 | 回答的问题 | 主要资产 | 一句话 |
|:---|:---|:---|:---|
| **投研层 Research** | 市场发生了什么？我需要研究什么？ | `Eyes/`、行情与事实数据源、外部 Skills、Reader | 获取与标准化事实，完成信息压缩和初始调研 |
| **策略内核层 Strategy Kernel** | 面对同一研究事实，我怎么判断？ | `Brain/`、Strategy Profile、状态与记忆、`BrainStorm/` | 决定判断方式、路由、记忆和改进规则 |
| **操盘层 Trading** | 判断形成后，我如何计划、确认、执行和留痕？ | Console 交易计划台、买卖决策台 + Human | 把计划、门禁与生命周期变成可执行流程；最终执行权属于人 |

### 新能力怎么归层

只问一个问题：**换掉它，会改变什么？**

- 只改变研究输入或报告，不改变个人判断方式 → 投研层。
- 同一研究事实会得到不同结论 → 策略内核层。
- 结论不变，但计划、操作、门禁或留痕方式改变 → 操盘层。

物理目录不等于语义归属。例如 `Console/rules.json` 位于 Console，但它会改变门禁结果，因此是策略内核配置在操盘层的投影；Reader 位于 Console 目录，但主要服务投研阅读。

## 2. 技术实现：三层之上有一份跨层运行协议

```text
Agents/（角色、依赖、权限、失败语义）
  ├─ Market Insight  → Eyes/市场洞察/
  ├─ Investment Radar → Eyes/投资雷达/
  ├─ Strategy         → Brain/策略分析/ 与待 Human 确认事项
  └─ Portfolio State  → 已确认事实的唯一 Agent 写回出口

Eyes/ + Skills
  → Brain/ + Strategy Profile
  → 持仓与关注/ + 买卖决策状态
  → Console/ + Human
  → BrainStorm/ 与复盘证据
  → Human 审批后的内核更新
```

`Agents/` **不是第四个业务层**，也不是 Python 服务或四个常驻进程。它是宿主 Agent 在运行各层工作流时必须遵守的协议：

1. 角色卡说明当前身份和写权限。
2. `EXECUTION_PLAYBOOK.md` 说明通用检查、成功与失败语义。
3. 具体 SOP 说明本次任务怎样执行。
4. Human 或定时任务负责触发；文件存在本身不会注册任务。

默认模式是 Single Controller / Multi-Role：一个主控按依赖顺序切换角色。宿主支持子 Agent 时，可以拆成多个执行体，但输入、输出、依赖和唯一写回边界不变。完整协议见 `Agents/README.md` 与 `Agents/ORCHESTRATION.md`。

## 3. 投研层边界

投研层负责：

- 拉取价格、新闻、公告、财务与用户允许的外部信息。
- 保留来源、时间、覆盖范围和失败原因。
- 输出可被内核消费的标准化事实。

投研层不负责：

- 把热门、上涨、回调或任何数据形态直接解释成买卖信号。
- 内置作者的选股偏好、仓位参数或退出规则。
- 在没有内核与 Human 的情况下生成交易动作。

当 Eyes 工作流调用 P 系列时，含义是“投研流程请求策略内核解释事实”，不是“研究报告自己做决定”。

## 4. 策略内核层构成

内核由三部分组成：

1. **判断**：Strategy Profile、P 系列与 SOP 路由。
2. **运行时记忆**：当前账户、持仓、Watchlist 与最近一次快照，回答“现在是什么”。
3. **进化语料**：交易计划与实际偏差、决策审计、单笔生命周期、平仓复盘和 BrainStorm，回答“过去怎么走到这里”。

跨层调度不属于某一个业务层，由 `Agents/` 单独定义角色、顺序、权限与唯一 Agent 写回出口。

### Strategy Profile 是唯一策略接口

公开版把“判断方法”和“判断参数”分开：P 系列完整开放作者实际使用的方法，每个模块标注 `invariant` / `profile` / `optional`，说明哪些是结构约束、哪些可以整体替换；所有具体数值由用户在 Strategy Profile 里定义，模块本身不带默认参数。作者的市场扩展模块另外标注 `disabled_by_default`。

用户通过 Strategy Profile 定义：

- 能力圈与市场范围。
- 入场论点和证据要求。
- 主要裁判与可证伪信号。
- 仓位和风险预算口径。
- 资金期限与论点期限。
- 加仓、持有、退出规则。
- 信息节奏与复核频率。
- Human 门禁与允许修改规则的时点。

机器接口见 `Brain/策略配置/strategy-profile.schema.json`；人类模板见 `Brain/策略配置/_模板_策略_Profile.md`。

## 5. 操盘层边界

Console 同时承载阅读入口和操盘工具：Reader 服务投研阅读；交易计划台、买卖决策台与 Human 构成操盘层。

- Reader 负责阅读，不改状态。
- 交易计划台负责把盘前判断和盘中动作分开。
- 买卖决策台负责建档、门禁、成交确认和生命周期留痕。
- Human 决定是否执行，并亲自完成真实操作。

Agent 可以读取操盘层产生的 Markdown，也可以提出建议，但不得代替用户生成“已经执行”的证据。Portfolio State 是**唯一 Agent 状态写回者**；Human 通过 Console 确认的真实事件不受这条限制，因为那是事实来源，不是 Agent 判断。

## 6. 内核如何改进

YMOS 不把历史记录直接“训练”成新规则。正确闭环是：

```text
策略内核层做出判断
  → 操盘层留下证据（论点、门禁、成交事件流）
  → 平仓后逐笔提取「计划 vs 实际」
  → 优化建议归档（Brain/内核审计/优化建议/YYYY-MM/）
  → 跨笔聚合，找重复出现的结构缺口
  → 形成判断层变更提案
  → Human 审批
  → 试运行
  → 到期复核
  → 验证、回滚或继续观察
  → 策略内核层更新，下一次判断时生效
```

闭环的两端都不能断：BrainStorm 与旧日志负责帮助用户梳理策略和发现矛盾；操盘证据负责验证真实判断与执行。策略内核的修改必须回到 Human 审批和后续复核，任何一种语料都不能自动成为规则。

任何内核修改都必须写明：触发证据、改前改后、预期效果，以及“如果这次修改是错的，会看到什么”。没有可证伪条件的修改不得进入稳定内核。

## 7. 开源边界

### 完全开放

- 三层架构、Agents 运行协议、数据契约和状态机。
- Strategy Profile Schema 与适配方法。
- 通用驱动、通用 Console、诊断与审计流程。
- 判断层变更协议和 Human 审批边界。

### 仅作为参考实例

- 作者或社区贡献的 Strategy Profile。
- 特定市场、流派或数据源的扩展工作流。

参考实例必须显式标注 `disabled_by_default`，不得成为公开版的隐含默认。

### 不进入公开仓库

- 真实账户、持仓、观察池、决策记录与个人参数。
- 作者专用定时任务、选股习惯和个人工作台。
- 面向特定网站的公开演示包、商业渠道资料和内部交接文档。

---

*YMOS V4 · 三层业务架构 + 跨层运行时协议*
