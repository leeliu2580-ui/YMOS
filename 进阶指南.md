# YMOS V4 进阶指南

> 前提：你已经跑通了 README 的 Step 0–3（市场洞察 → 投资雷达 → 标的管理与 Reader）。
> 还没跑通，先回 [README](README.md) 走完上手指南——**这份文档不重复讲怎么起步**。

跑起来之后，YMOS 按三层逐步成长，最后再用证据把它们连成闭环：

```text
① 补投研层      需要看见和研究什么
② 装策略内核    面对同一事实，你怎么判断
③ 调操盘层      判断形成后，怎么计划、过门禁和留痕
④ 按证据进化    让思考与真实结果回到策略内核
⑤ 用好 Agent    一个统筹长期状态，多个工具执行具体任务
⑥ 扩展系统      按自己的参数改投研、策略、操盘与看板
```

### 新用户与 V3 用户从不同位置开始

| 你处在哪个阶段 | 从哪里开始 |
|:---|:---|
| 第一次接触 YMOS | 先按 README 跑通投研层，用 Reader 看一段时间真实产出，再进入 §2 |
| 从 V3 升级 | V3 的市场洞察、投资雷达和标的管理就是投研基础；验证迁移后，可以直接进入 §2 梳理策略内核 |
| 暂时不想用操盘台 | 完全可以完成 §2，并用 BrainStorm / 旧日志持续梳理；§3 不是前置条件 |
| 已经积累真实交易记录 | 完成 §2 后接 §3，再用 §4 做结果验证和周期审计 |

对新用户，先跑投研层不是因为策略内核不重要，而是需要真实产出帮你提出具体问题——「为什么雷达推给我的东西总不对味」「为什么我写的规则没有可观察的失效信号」。有了问题再配置 Profile，比 Day 0 凭空填写更诚实。

一上来就问「你的失效信号是什么」，大多数人只能编。

| 你处在哪个阶段 | 该看哪一节 |
|:---|:---|
| 刚跑通最小闭环，想接更多数据或稳定调度 | **§1 投研层** |
| 想把已有投资逻辑、旧日志和复盘整理成系统 | **§2 策略内核层** |
| 判断相对清楚，但计划和真实操作总是走样 | **§3 操盘层** |
| 攒了十几笔交易，想让系统自己变好 | **§4 按证据进化** |
| 想选择 Agent、接 Skills 或让 AI 帮你改系统 | **§5 Agent 使用** |
| 想按自己的市场、策略与习惯整体升级三层 | **§6 系统级扩展** |

---

## 一、投研层：补你真正需要的信息

投研层负责看市场、取事实、做初始调研和形成研究输入，也最容易过度建设。**先问一句：多这个数据源或报告，我的研究会因此变好吗？** 不会就别接。

### 1.1 数据源分级：先解决问题，再加 Key

| 级别 | 配置 | 解锁 | 什么时候需要 |
|:---|:---|:---|:---|
| Level 0 | 无需 Key | RSS + Yahoo Finance 价格兜底 | 所有人先从这里跑通 |
| Level 1 | `FINNHUB_API_KEY` | 美股/Crypto 报价与个股新闻 | 主要研究美股或 Crypto |
| Level 1+ | `TUSHARE_TOKEN` | A股日线与最近交易日价格 | 主要研究 A 股 |
| Level 1++ | 问财 Skill + `IWENCAI_API_KEY` | A 股 What's Hot / 美股条件选股；其他事实查询为实验性入口 | 需要主动筛股时 |
| Level 2 | `YMOS_MARKET_API_*` | 已清洗、分类、去重的结构化市场事件 | RSS 噪音或本地处理成本已经成为瓶颈 |

Level 不是质量排名，也不要求按顺序全部安装。它回答的是“这个数据缺口该用什么补”：只做 A 股不必为了层级完整去配 Finnhub；只跟踪固定持仓，也未必需要条件选股。

统一配置方式：

```bash
cp .env.example .env
```

然后只填写需要的字段。现有脚本会从 YMOS 根目录自动读取 `.env`，不需要安装 `python-dotenv`。

> `.env` 已被 Git 忽略，但“被忽略”不等于可以随意复制。不要把真实 Key 写进 SOP、Markdown、业务脚本、定时任务提示词、聊天记录或命令参数；命令参数可能进入 shell history。

#### Level 0：RSS + Yahoo Finance，先验证最小闭环

不申请任何账户即可使用：

- `fetch_rss.py` 拉取公开 RSS，供市场洞察做事件归纳；
- `fetch_price_router.py` 在没有专用 Key 时回退 Yahoo Finance；
- 市场洞察、投资雷达、Reader、Strategy Profile 和 BrainStorm 都不依赖付费数据源。

最小测试：

```bash
python3 Eyes/scripts/fetch_rss.py 1 --output /tmp/ymos_rss.json
python3 Eyes/scripts/fetch_price_router.py \
  --symbols "AAPL,000001.SZ,0700.HK" \
  --output-dir /tmp/ymos_prices
```

Level 0 的价值是验证目录、SOP、路由和报告链是否正确。它的限制也要接受：RSS 可能失效或延迟，Yahoo 是免费兜底，不应被当作交易所级实时行情。单个源失败时，报告应该留下错误和降级说明，而不是假装数据完整。

#### Level 1：Finnhub，美股 / Crypto 的报价和事件增强

它主要补两类信息：

| 能力 | 没有 Finnhub | 配置后 |
|:---|:---|:---|
| 美股 / Crypto 价格 | Yahoo Finance 兜底 | Finnhub 报价 + Yahoo 互备 |
| 个股新闻 | 主要依赖通用 RSS | 可按持仓 Ticker 拉取 company news |
| 财报等事件 | 依赖其他新闻源发现 | 可作为事件检测补充输入 |

获取与配置：

1. 前往 [Finnhub](https://finnhub.io/) 注册；
2. 登录 Dashboard，复制账户的 API Key；
3. 在 `.env` 填写：

```env
FINNHUB_API_KEY=你的Key
```

测试：

```bash
python3 Eyes/scripts/fetch_price_router.py \
  --symbols "AAPL,NVDA" \
  --output-dir /tmp/ymos_finnhub

python3 Eyes/scripts/fetch_finnhub_news.py \
  --hours 24 \
  --output /tmp/ymos_finnhub_news.json
```

免费额度、延迟和端点权限可能调整，以 Finnhub 当前 Dashboard 为准。YMOS 的日频扫描通常不需要高频调用；不要为了“更实时”把投研任务改成无意义的分钟级轮询。

#### Level 1+：Tushare，补 A 股日线与最近交易日价格

Tushare 解决的是 A 股代码和日线稳定性问题，不把它描述成券商级实时行情。

获取与配置：

1. 前往 [Tushare Pro](https://tushare.pro/) 注册；
2. 在个人中心复制 Token；不同接口所需积分以官网当前规则为准；
3. 在 `.env` 填写：

```env
TUSHARE_TOKEN=你的Token
```

测试：

```bash
python3 Eyes/scripts/fetch_price_tushare.py \
  --symbols "688981.SS,000001.SZ" \
  --output /tmp/ymos_tushare.json
```

价格路由会按 Ticker 自动分流：

```text
.SS / .SZ → Tushare；无 Token 时回退 Yahoo
.HK       → Yahoo Finance
其他      → Finnhub；无 Key 时回退 Yahoo
```

Finnhub 与 Tushare 可以独立配置。两市都研究就都配；只做一个市场就只配对应的数据源。

#### 问财数据层（可选 · Beta）

V4 起新增，让投研层从“只能取价格和新闻”变成“**能做条件选股和事实检索**”——这是研究触角变长的一层，不替用户改变策略规则。

它不是 YMOS 内置 Skill，需要两步：

1. 从[同花顺问财官方 SkillHub](https://www.iwencai.com/skillhub)安装对应 Skill；
2. 配置 `IWENCAI_API_KEY`，必要时再指定 Skill 根目录。

推荐安装流程：

1. 登录 SkillHub，打开所需的官方 Skill；
2. 在安装方式中选择 Agent 用户，把官方生成的安装 Prompt 交给你的 Agent；
3. 按 SkillHub 当前说明获取 `IWENCAI_API_KEY`；
4. 优先安装到 Agent 自己的标准 Skills 目录。装在其他位置时，把“包含各 Skill 文件夹的上级目录”写入 `YMOS_SKILL_ROOT`；
5. 安装后同时确认宿主能识别 Skill、YMOS 能找到 `scripts/cli.py`。

```env
YMOS_SKILL_ROOT=/包含各个Skill文件夹的上级目录
IWENCAI_API_KEY=你的问财服务Key
IWENCAI_BASE_URL=https://openapi.iwencai.com
```

`YMOS_SKILL_ROOT` 不是 Key。它只告诉 YMOS 去哪里找外部 Skill；如果 Skill 已安装在自动搜索目录，可以留空。

自检：

```bash
python3 Eyes/scripts/skill_resolver.py

python3 Eyes/scripts/iwencai_stockpick.py \
  '流通市值大于100亿，今日涨幅大于8%，非ST' \
  --json-out /tmp/ymos_iwencai.json \
  --csv-out /tmp/ymos_iwencai.csv \
  --desc-out /tmp/ymos_iwencai_query.txt \
  --status-out /tmp/ymos_iwencai_status.txt \
  --limit 5 --no-fetch-all
```

V4 已用真实 Key 验证 A 股与美股条件选股封装。财务、研报需要各自对应 Skill；公告、新闻当前由封装脚本直连接口，这四项仍按实验性入口管理。港股官方 Skill 可以由 Agent 直接使用，但公开版尚未提供港股 What's Hot 自动链。

这些外部 Skill 不属于 YMOS，YMOS 不复制、不二次分发，也不替 SkillHub 猜下载地址。没安装不阻塞主链——依赖问财的 SOP 会明确写出缺的是 Skill、路径还是 Key。

#### Level 2：YMOS Market Data API，直接消费结构化市场事件

这是作者自用的数据管道对 YMOS 提供的结构化入口。当前底层以 28 源处理管道为基础，对中英文市场信息做抓取、清洗、分类与去重，减少本地处理 RSS 的 Token 消耗和中文源维护成本。来源概览见 [TIB RSS 信息源清单](https://yongmai.xyz/tib-rss-list/)；实际来源会随可用性调整。

| 维度 | 免费 RSS 路径 | Market Data API |
|:---|:---|:---|
| 输入 | 多个原始 Feed | 服务端预处理后的结构化事件 |
| 中文信息 | 取决于公开 RSS / 自建 RSSHub | 包含作者自用的中英文来源管道 |
| 本地处理 | Agent 负责较多清洗、去重和分类 | 本地直接消费结构化结果 |
| 失败降级 | RSS 单源失败后继续 | API 失败时回到 RSS 主链 |

自助获取：

1. 前往 [yongmai.xyz 注册页](https://yongmai.xyz/register-2/)注册或登录；
2. 在用户中心自助开通并生成专属 API Key；
3. 在 YMOS 根目录 `.env` 填写：

```env
YMOS_MARKET_API_URL=https://yongmai.xyz/wp-json/tib/v1/reports
YMOS_MARKET_API_KEY=你的Key
```

测试：

```bash
python3 Eyes/scripts/fetch_market_api.py 1 \
  --output /tmp/ymos_market_data.json
```

先用 Level 0 跑通市场洞察与投资雷达，再判断结构化 API 是否真的减少了你的噪音和处理成本。API 是可替换数据源，不是 YMOS 的运行前提；服务不可用或用户不配置时，应回退 RSS，而不是让整条投研链停摆。

#### 自定义 RSS 与更多 API

默认 RSS 不够时，先复制补充配置，不要直接改公共源文件：

```bash
cp Eyes/scripts/rss_sources_custom.json.example \
   Eyes/scripts/rss_sources_custom.json
```

`rss_sources_custom.json` 已被 Git 忽略。市场洞察会把补充 RSS 与主数据源并行读取，适合行业博客、公司工程博客或特定研究者。

如果要接新的金融 API，参考 `fetch_finnhub_news.py` 或 `fetch_price_api.py`：从 `.env` 读取凭据，输出带来源和时间的 JSON，在 SOP 里声明失败降级，再用正常、空结果和失败三个样本验证。不要让新数据脚本直接输出买卖结论。

#### 配完之后怎么判断是否成功

不要只看“命令没有报错”。至少确认：

1. 输出文件真实存在且能解析；
2. `source`、时间与市场字段正确；
3. 空结果和接口失败会留下明确状态；
4. 投资雷达实际读取了新数据，而不是仍在使用旧缓存；
5. 删除或清空 Key 后，系统能按文档回退，而不是静默生成假报告。

### 1.2 一个合格驱动的职责

只做四件事：取事实、记来源和时间、输出结构化结果、说明失败降级。

建议输出：

```json
{
  "schemaVersion": 1,
  "source": "provider-name",
  "asOf": "YYYY-MM-DD HH:mm",
  "market": "",
  "symbols": [],
  "facts": [],
  "unresolved": [],
  "errors": []
}
```

投研脚本和数据适配器**不许**写“应该买”“应该卖”或任何个人策略结论。它们可以做事实归纳和初始研究；根据 Profile 形成个性化动作判断，是策略内核层的工作。

### 1.3 加一个新驱动

1. 在 `Eyes/scripts/` 写单一职责脚本（照着 `fetch_rss.py` 或 `fetch_price_api.py` 的模式）。
2. 输出标准 JSON，不带策略结论。
3. 在对应 Eyes SOP 里声明输入、输出、失败降级。
4. 用三个样本测：正常、空、失败。
5. 在 Profile 的驱动清单里标为可选，不默认启用。

### 1.4 数据新鲜度

Profile 里写明各类事实的有效期。**Agent 不能把旧快照当实时行情用。** 需要精确金额、止损判断或再平衡时必须先刷新——过期就只提示刷新，不给动作结论。

### 1.5 调度：写依赖，不抄时间表

公开版只定义顺序：

```text
外部事实就绪 → 投资雷达映射 → 策略分析/持仓体检
  → Human 审阅 → 盘前计划 → 盘中执行与留痕
```

具体时间由你的市场、时区和 Profile 决定。一条调度至少写清：时区与触发条件、上游产物、读取范围、允许写入的路径、空数据与失败怎么处理、Human 要审阅什么。

**不要因为工具支持 cron，就默认所有模块都该自动跑。** 一个健康的配置，长期启用的任务在 4–6 个。

> 一套真实跑过几个月、迭代过二十多版的参考配置，含时序依赖、踩坑约束和「什么该停掉」，见 [`Agents/SCHEDULES_REFERENCE.md`](Agents/SCHEDULES_REFERENCE.md)。
> 直接抄时间没意义，值得抄的是**为什么这么排**——尤其那条「把产生决策的任务，全部挪到收盘后」。

---

## 二、策略内核层：装你自己的判断

策略内核层决定这套系统为什么像你。这一节是整份指南的重点。

### 2.1 不要从改提示词开始

最常见的错误：一上来就改 P 系列。

```text
❌ 改提示词 → 感觉不对 → 再改 → 系统变成一堆临时补丁
✅ 说清策略 → 诊断一致性 → 写 Profile → 映射到 P/SOP → 投影到 Console 门禁
```

如果连入场、认错、仓位、期限都说不清楚，改提示词只会把一个模糊策略自动化——跑得更快，错得更整齐。

### 2.2 第一步：让诊断把你说不清的东西逼出来

```text
帮我做一次投资诊断
```

诊断不问"你是什么派"，它问的是能对照证据检验的东西：

- 你的策略能不能用三句话复述？
- 入场逻辑、认错逻辑、仓位逻辑，是不是同一套？
- 每条规则能不能改写成"出现什么我就认为我错了"？
- 哪些决定必须在收盘后做完？
- 有没有一份不能被情绪事后改写的记录？

**最常见的诊断结果不是"你的参数不对"，是"你说的和你做的不是一套"。** 嘴上说价值投资，实际买入理由是它在涨，止损用的是趋势派的方式——这种矛盾在第一次回撤时一定会爆。

诊断有两种用法：

| 用法 | 输出 | 适合 |
|:---|:---|:---|
| 独立 `ymos-diagnosis` | 一份诊断报告（读完就完） | 还没有系统和历史数据 |
| YMOS 内集成 | 可写入的配置草案 | 已经装了 YMOS |

区别是本质的：**一次对话读完就忘了，一份写进 Profile 的配置每天都在生效。**

### 2.3 第二步：写 Strategy Profile

```text
配置一下我的内核
```

Agent 会跑 [`Brain/内核审计/SOP_内核入职配置.md`](Brain/内核审计/SOP_内核入职配置.md)，把诊断结论映射成草案。

Profile 的字段就是内核的全部：

| 字段 | 你要回答 |
|:---|:---|
| `scope` | 做什么市场和工具、能力圈在哪、明确不做什么 |
| `decision.entry` | 一笔机会成立需要什么证据；什么证据不能单独触发动作 |
| `decision.invalidation` | **出现什么，说明我错了**（这一条最关键） |
| `decision.add / exit` | 什么情况可以加仓、必须退出 |
| `risk` | 仓位口径、单笔损失预算、论点周期、资金可用期限 |
| `evidence` | 必须用哪些事实源、信源优先级、多久算过期 |
| `cadence` | 多久监控一次、什么时间窗口允许形成新判断 |
| `execution` | 哪些步骤必须 Human 确认、盘中禁止改什么 |
| `modules` | 启用 / 停用 / 替换了哪些 P 和 SOP |

机器定义见 [`strategy-profile.schema.json`](Brain/策略配置/strategy-profile.schema.json)。

**所有数值由你填。** Agent 不许从作者案例、旧模板或"常识"里补默认值。写不出来的字段就留空——空白是诚实的，编一个数字不是。

### 2.4 六类结构参照：定位，不是选择题

[`strategy_family_map.md`](Brain/ymos-diagnosis/references/strategy_family_map.md) 列了六类常见结构：价值/均值回归、成长/质量、趋势/动量、事件驱动、短周期/微观结构、套利/相对价值。

它只帮你回答四件事：主要证据是什么、主要裁判是什么、什么时候认错、需要什么信息频率。

**混合策略是允许的**——大多数人都是混的。但混合有一个硬要求：

> 不同裁判冲突时，必须写明谁优先。

举个真实的冲突：你用基本面选股（价值），但用价格止损（趋势）。股价跌了 20%，基本面没变。这两套逻辑会打架，而**你会在最难受的时刻临场决定听谁的**——那就是系统失效的瞬间。Profile 里提前写死谁优先，这个瞬间才不会出现。

### 2.5 第三步：把 P 系列映射到你的 Profile

P1–P18 完整开放，每个都标了归属（见 [README 的标注制说明](README.md#p-系列完整开放但每一项都标了归属)）。映射时逐个过一遍：

| 看到标记 | 你该做什么 |
|:---|:---|
| `invariant` | 保留。要改就写明理由——这些是所有策略族都需要的结构 |
| `profile` | **重点看这里**。读模块头部那句"如果你不是这个派该往哪改"，然后动手改 |
| `optional` | 跟你的策略无关就停用，删掉不影响主闭环 |

改完在 Profile 的 `modules` 里记下来：启用了什么、停用了什么、替换了什么。这份清单以后是周期审计的输入。

**不要把某个流派的经验写成系统公理。** 你自己的经验也一样——它是你的 `profile`，不是别人的 `invariant`。

### 2.6 用旧日志与 BrainStorm 补全内核上下文

配置策略内核不以操盘层为前置。你已经写过的投资日志、复盘记录、长期原则，以及 BrainStorm 里的 Raw / Insight，都是理解“你为什么这样判断”的重要材料。

资料很多时，先把副本放进 `BrainStorm/历史投资资料/原始资料/`，运行 [`SOP_历史投资资料入职.md`](BrainStorm/SOP_历史投资资料入职.md)。正确顺序不是“把所有文档一次塞给模型做总结”，而是：

```text
盘点文件 → 建资料索引 → 按时期 / 主题画地图
  → 抽取代表性成功、失败与矛盾样本
  → 形成阶段梳理 → 诊断时按问题读取相关原文
```

原件保持不变，资料索引和梳理报告保存在 `历史投资资料/`，并默认被 Git 忽略。日常头脑风暴只读 MEMORY、STATUS 和近期 Insight；只有当前问题相关时才沿索引打开旧日志，避免上下文被历史材料淹没。

把它们交给 Agent 时，要求分成三类：

| 类型 | 怎么处理 |
|:---|:---|
| 已经反复验证、目前仍明确认可 | 写入 Profile 草案，仍需 Human 逐条确认 |
| 反复出现但互相矛盾 | 列为诊断问题，找出不同策略族或不同市场下的适用边界 |
| 一次性感悟、情绪或尚未验证的猜想 | 留在 BrainStorm，作为候选规则，不直接生效 |

这一步的价值不是“从笔记里自动训练策略”，而是把散落的投资思想变成可讨论、可证伪的结构。即使你从不使用 Console 两个操盘台，也可以长期进行这类梳理。

特别注意区分三种时间口径：**当时明确写下的观点、事后给结果的解释、今天仍然认可的规则**。它们不能合并成同一句“用户一直认为”。

### 2.7 想用作者扩展？先做映射

仓库里这组模块全部 `disabled_by_default`：

| 扩展 | 作用 | 位置 |
|:---|:---|:---|
| P17 | 核心资产、稀缺性、终局空间与股性判定 | `Brain/references/p17-core-asset.md` |
| P18 | 从每日价格异动生成研究候选 | `Brain/references/p18-daily-mover-scanner.md` |
| P9+P17 专项调研 | Human 点名后的深度研究链 | `Brain/SOP_单标的P9P17专项调研.md` |
| 热门追踪 | A 股 What's Hot、美股热门事件发现 | `Eyes/SOP_Whats_Hot.md` 等 |
| 资产库治理 | 自动建档、核心资产动态判定、回踩提醒 | `Eyes/SOP_Watchlist自动建档.md` 等 |
| 配套扫描脚本 | 为上述 SOP 提供候选池和价格数据 | `Eyes/scripts/` |

这组研究与执行链在真实资金上被完整跑过一个有效周期。它是一条有血有肉、值得研究的进化路径，但**这不能证明某个模块有独立因果效力，也替代不了你自己的样本验证**。

启用步骤：

1. 复制 [`module-manifest.example.json`](Brain/策略配置/examples/author-profile/module-manifest.example.json) 到自己的私有配置区。
2. 把模块里的市场范围、资产标签、评分权重、阈值逐条映射到你的 Profile。
3. 删掉不属于你能力圈的规则；没确认的保持关闭。
4. 手动跑 `P18/热门追踪 → P17/P9 研究 → P5/P12 审计`，检查输入输出和降级行为。
5. **手动链稳定之后**，才按 [`Agents/SCHEDULES.md`](Agents/SCHEDULES.md) 的依赖契约配自己的时间表。仓库不提供作者的真实调度。

正确用法是「参考它如何长出一个内核」，不是把整套复制成你的默认答案。

---

## 三、操盘层：装拦住你自己的闸门

策略内核决定你怎么想，操盘层决定**想清楚之后你会不会照做**。

大多数人的亏损不来自判断错，来自判断对了但没执行。所以这一层的设计目标只有一个：**让违规变得需要额外动作。**

### 3.1 三条已经内建的结构

启动 Console 就有，不用配置：

| 结构 | 拦的是什么 |
|:---|:---|
| **计划与执行分离** | 盘前定调、盘中只执行。防止你在盘面刺激下重新发明规则 |
| **append-only 事件流** | 原始论点和每次事件只追加不覆盖。防止你事后把"我早就知道"写进历史 |
| **门禁必须显式通过** | 结构性红线（仓位、期限、最大亏损）由页面计算，不给你"这次算了"的入口 |

第二条特别重要：**能被改写的记录，等于没有记录。** 你三个月前写的买入理由必须原样躺在那里，周期审计才有东西可读。

### 3.2 把你的红线投影到 Console

`Console/rules.json` 是 Profile 在操盘层的**投影**，不是第二套策略真相源。

```bash
cd Console
cp rules.example.json rules.json
```

改的时候记住分工：

| 页面可以决定 | 页面不能决定 |
|:---|:---|
| 字段布局、操作顺序 | 你属于什么策略 |
| 提示方式、哪些需要显式确认 | 什么阈值是对的 |
| 清单里问哪些问题 | 哪条论点应该成立 |
| — | 是否绕过 Human 最终确认 |

**不要同时维护一套 Profile 和一套跟它矛盾的 `rules.json`。** 冲突时以 Human 确认过的 Profile 为准。

### 3.3 从你自己的错误反推该装哪道闸门

这一步 Day 0 做不了——你在这套系统里还没犯过错。跑够样本之后再回来。

方法是倒推：

```text
找出重复发生的那类偏差
  → 问：它第一次变得可见，是在哪一步？
  → 闸门装在那一步，不是装在亏损发生的那一步
```

举例：如果你反复"计划买 10% 仓位，实际买了 20%"，闸门不该装在成交确认（太晚了），该装在建仓准备——让金额超出计划时必须写一句理由。

**别照抄别人的闸门。** 我的闸门拦的是我的弱点，你的弱点大概率不一样。

### 3.4 加新工作台

想加自己的页面，先写清六件事：

1. 消费哪个真相源
2. 能写哪些目录
3. 哪些字段只能追加
4. 哪些事实可以更新
5. Human 在哪一步确认
6. 失败后怎么恢复

铁的边界：**任何"已执行""已成交""已验证"状态，都必须来自真实的人的操作。** Agent 不能为了让流程看起来闭环而补写这些。

---

## 四、积累语料与样本之后：按证据改内核

这是 YMOS 和"一堆提示词"的真正区别。

### 4.0 先看清这个闭环

```text
旧日志 / BrainStorm → 梳理信念与矛盾 → 候选规则
                                            ↓
策略内核形成判断 → 操盘层留下证据 → 平仓后逐笔提取 → 优化建议归档
   ↑                                                           ↓
   └── 策略内核更新 ← Human 审批 ← 判断层变更提案 ← 跨笔聚合找重复
```

两条路径都重要：认知语料帮助你说清策略，操盘证据帮助你验证策略与执行。**没有操盘层，也可以整理内核；有了真实结果，才更适合判断某条规则是否真的有效。**

闭环不能断。策略内核的修改不能只来自一时感觉；操盘层的证据也要回到策略内核，否则只是一堆没人读的记录。

大多数人的系统断在两个地方：要么记录了但从来不读（证据不回流），要么凭感觉改规则（修改没有证据）。这一节讲怎么把两头接上。

### 4.1 每笔平仓后：逐笔提取证据

```text
复盘一下 [ticker]
```

跑 [`SOP_平仓复盘归档.md`](Brain/内核审计/SOP_平仓复盘归档.md)，把这一笔的计划与实际提取出来，落进 `Brain/内核审计/优化建议/YYYY-MM/`。

**为什么这是最强的证据**：单笔决策文件从建档到平仓，完整保留了你当初怎么想、后来做了什么、最后发生了什么。别的语料靠回忆，这一份靠事件流——所以有三件事可以被机械检出，不需要你诚实：

| 红旗 | 怎么检出 | 该往哪修 |
|:---|:---|:---|
| **放宽止损** | `adjust` 事件里新止损离成本更远 | 操盘层门禁：调整时强制写“这条证据建仓时是否已知” |
| **亏损加仓** | `add` 事件时价格低于当时成本 | 操盘层门禁：浮亏状态下 `add` 需额外确认 |
| **绕过门禁** | 决策审计里有拦截但仍然成交 | 先查是规则不合理还是人不服气——两种修法相反 |

这三项**和盈亏无关，赚钱了也照记**。赚钱的违规比亏钱的违规更危险，因为它会被合理化成经验。

复盘时判断力和执行力必须分开问：

- **判断力有问题**（论点错了、失效信号写得没法观察）→ 可能要改内核。
- **执行力有问题**（想清楚了但没照做）→ **优先加操盘层门禁，不要动策略。**

把这两者混在一起，最常见的后果是：因为没执行到位而亏钱，却回头把策略改松了。

> **单笔复盘不改任何内核文件。** 它只负责把证据固定下来。

### 4.2 周期审计：跨笔找重复

```text
审计一下我的内核
```

**由定时任务驱动，建议月度或季度跑一次，不要每周跑。** 审计的节奏该由日历决定，而不是由"我最近亏了想改点什么"决定。

下面的门槛只约束**由交易结果驱动的内核修改**：

| 已归档平仓笔数 | 这一轮该产出什么 |
|:---:|:---|
| **< 10 笔** | 只做观察登记，明确写"样本不足，不提案" |
| **≥ 10 笔** | 可以提案，每个提案必须点名依据的具体笔数 |
| **≥ 30 笔** | 可以做分策略族、分市场的横向对比 |

10 笔是经验下限，作用是**挡住"三笔就想改规则"**——三笔里连续两次同类问题，很可能只是运气。

审计读已归档的复盘 + 计划与实际的偏差 + 决策审计 + 相关 BrainStorm，找**重复发生的结构缺口**，不复盘单只股票。

先检查归档完整性：`已平仓/` 里有、`优化建议/` 里没有的笔数，就是审计的盲区。

**所有结论都要放回你 Profile 的策略族里看。** 同一个现象在不同派别里含义相反：价值派"跌了不走还加仓"可能合理（前提是基本面未被证伪），趋势派做同样的事就是严重违规。用你自己声明的标准量你自己，不是用作者的。

八项里最容易被跳过、但最关键的是**反馈回路**：上一轮批准的变更，这一轮真的生效了吗？改了规则但同类错误没减少，说明改错了地方——通常是把执行问题当成了策略问题。

### 4.3 改之前先分类

亏了就改规则，是这类系统最典型的死法。系统会朝着"越来越会同意你"的方向进化，而且比没有系统更危险，因为它现在有系统的外观。

所以先分类：

| 原因 | 怎么处理 |
|:---|:---|
| 策略按设计运行，但结果不利 | **记录，不改规则。** 这是策略的正常波动 |
| 执行偏离了已有规则 | 优先加**操盘门禁**，不是改策略 |
| 规则表达不清或互相冲突 | **只有这类需要改内核** |
| 投研数据缺失、过期、失真 | 修数据源、脚本和研究流程 |

### 4.4 判断层变更提案

先判断是哪一类提案：

| 类型 | 典型来源 | 最低要求 |
|:---|:---|:---|
| **规则澄清型** | 结构诊断、旧日志、BrainStorm 暴露出明确歧义、互相冲突或真相源不一致 | 保留原规则，指出冲突，写明新表述、反证、试运行和回滚；可以不等 10 笔，但只能标记 `trial` |
| **结果验证型** | 平仓归档和周期审计显示同类判断/执行缺口反复出现 | 遵守 §4.2 的跨笔门槛，点名证据笔数与笔号 |

规则澄清型解决“系统连自己在说什么都不一致”；结果验证型解决“这套说法放进真实操作后是否有效”。前者不能冒充后者。

每个改动单独走 [`_模板_判断层变更提案.md`](Brain/内核审计/_模板_判断层变更提案.md)，必须写全：

触发证据 · 结构缺口 · 改前 · 改后 · 预期改善 · **如果这次改错了会看到什么** · 复核时间或样本量 · Human 审批 · 回滚方案

倒数第四项是关键——它就是"可证伪化"用在改动本身上。**没有反证条件的改动，下次审计自动标为"未验证"，不进稳定内核。**

不要把多个改动混成一个大提案，那样验证不了是哪一条起了作用。

### 4.5 BrainStorm：语料不是规则

BrainStorm 首先是个人投资思考与复盘工作区：保存旧投资日志、Raw 思考、当前状态、近期 Insight、长期 MEMORY 和规则候选。它帮助用户形成持续记录和回看的习惯；内核优化只是其中一条按需反馈回路，不是每次记录的目的。

一条感悟反复出现，只说明它**值得审查**，不说明它自动成为内核规则。若它揭示的是明确的规则歧义或冲突，可以发起规则澄清型提案；若它声称某个判断更有效，则必须等真实结果验证。两条路都要说明结构缺口、写明反证和回滚，并由 Human 审批。

另一条同等重要的证据路径来自 `Brain/买入卖出决策/`：交易计划、决策门禁、单笔事件流和平仓复盘记录“实际怎么做、结果怎样”。BrainStorm 记录“怎么想”，买卖决策记录“怎么做”；二者在周期审计处汇合，经 Human 审批后才反馈到策略内核。

跑三个月之后，这些变更记录会变成一样别人拿不走的东西：**你自己的判断进化史。** 代码可以被 fork、被抄、被更好的项目替代；"我怎么一步步变成能拿住的人"这份记录不能被迁移。

---

## 五、Agent 使用：统筹一个，执行可以多个

YMOS 不要求特定 Agent 品牌。最低能力只有两项：**能读写本地文件、能运行 Python 脚本**。但如果想长期使用，还要考虑记忆、调度和权限边界。

### 5.1 先做能力自检

第一次把 YMOS 交给某个 Agent 时，让它明确回答：

| 能力 | 要检查什么 | 缺失后的影响 |
|:---|:---|:---|
| 本地文件 | 能否递归读取 YMOS、写入指定目录、保留中文路径 | 无法稳定读取 SOP 或写回报告 |
| Python | 能否运行 `Eyes/scripts/*.py` 与 `Console/server.py` | 数据获取和 Console 无法完整运行 |
| Web 搜索 | 能否访问最新公告、财报和新闻，并保留来源 | 深研只能依赖用户提供材料与现有数据源 |
| Skills / 工具 | 能否安装和调用外部 Skill，能否管理 Secret | 问财等可选能力需要改为手动调用或停用 |
| 定时任务 | 能否固定工作目录、时区和依赖顺序 | 日常投研链只能由 Human 手动触发 |
| 长期记忆 | 会话结束后是否还能读取同一状态 | 必须更严格地把结论写回 Markdown 真相源 |
| 多角色 / subagent | 能否把有边界的任务分给不同执行体 | 不支持也不阻塞，主控按顺序扮演角色即可 |

缺一项不代表不能用。关键是**明确降级**，不要让 Agent 假装已经搜索、安装、写回或执行。

### 5.2 作者当前的实际组合

可以把 Agent 工具分成两个角色，避免和 YMOS 的“操盘层”混淆：

| 角色 | 怎么选 | 作者当前用法 |
|:---|:---|:---|
| **运行统筹** | 负责长期记忆、任务协调、定时任务与状态连续性，建议只选一个 | 当前使用 [Hermes](https://hermes-agent.nousresearch.com/) 统筹 YMOS；OpenClaw 同样支持完整主链与调度 |
| **任务执行工具** | 按一次深研、一次改造、一个脚本或一条调度任务临时调用，可以多个并存 | Claude Code、Codex、OpenClaw 等按任务分担，产出统一写回 YMOS |

为什么统筹建议只有一个：两套框架各自保存一半记忆，会形成两个互相不知道对方存在的“当前状态”。任务执行工具可以换，因为真相源不是对话记录，而是磁盘上的 Profile、状态机、报告和事件流。

只使用 OpenClaw、Claude Code、Codex 或其他单一 Agent 也能完整跑 YMOS；没有常驻统筹时，要更主动地维护 `PROJECT_STATE`、Profile、BrainStorm 和状态机，不能把关键决定只留在聊天窗口。

### 5.3 Agents 目录不是四个自动运行的机器人

`Agents/` 定义的是角色协议：谁先读什么、能写哪里、何时算完成。它不会因为文件存在就自动注册任务，也不要求启动四个进程。

```text
Market Insight       只看市场，写市场洞察
Investment Radar     把市场事件映射到持仓与 Watchlist
Strategy             按 Profile 和 P 系列分析
Portfolio State      统一状态写回，维护真相源
```

- 单 Agent：按 `ORCHESTRATION.md` 的顺序依次扮演。
- 支持 subagent：可以把四个角色映射成不同执行体，但写回权限不变。
- 定时任务：提示词必须同时指向角色卡、执行手册和具体 SOP；只写一句“跑投资雷达”不足以激活角色边界。

### 5.4 Agent 的模型怎么分配

模型名称变化很快，不在公共文档写死。按任务风险分配即可：

| 任务 | 模型选择 |
|:---|:---|
| RSS 拉取、格式转换、文件归档、状态校验 | 稳定的轻量模型即可 |
| 市场洞察、投资雷达、常规建档 | 中高能力模型，要求长上下文和来源意识 |
| 策略诊断、P5/P6/P9/P12、深度调研、内核变更 | 尽量使用当前可稳定获得的强模型 |

强模型也不获得额外权限。事实仍要有来源，规则仍要可证伪，真实成交与内核批准仍由 Human 完成。

### 5.5 让 Agent 帮你改 YMOS

用户不需要先学会写代码，但要能说清**希望系统哪里变得更像自己**。可以把这段发给 Agent：

```text
先完整读取 YMOS/AGENT_GUIDE.md、YMOS/进阶指南.md、ARCHITECTURE.md，
以及与本次需求有关的 Profile、SOP 和 Console 文档。

我要解决：<实际问题>。
我的市场、周期和使用习惯：<参数>。

请先判断应修改投研层、策略内核层、操盘层，
还是只修改 Reader / Console 的显示；列出真相源和受影响的上下游，
再给最小改造方案。保留 Human in the Loop、失败降级、事件只追加；
真实 Key 只放 .env。修改后做正常、空结果、失败三个测试，不要直接发布或 push。
```

Agent 应先给出“改哪层、为什么、哪些文件受影响”，再动文件。只说“帮我优化 YMOS”通常会造成跨层大改和新的冲突。

---

## 六、系统级扩展：按你的参数升级三层与看板

V4 提供的是作者打样，不是一套要求用户照抄的成品。真正的系统级升级，是先把你的参数说清，再分别投影到投研、策略和操盘层。

### 6.1 先准备一份个人参数包

不需要一次写满，但至少把已知与未知分开：

```text
市场与时区：
主要标的 / 明确不做：
研究频率与可投入时间：
持有周期与资金期限：
主要策略结构：
入场依据：
失效信号：
仓位与损失预算：
已知行为弱点：
希望自动化的重复劳动：
希望 Human 必须确认的节点：
Reader 想看什么：
操盘台想怎么排字段和提醒：
尚未想清、必须留空的参数：
```

来源优先级：Human 明确确认的 Profile → 实际计划与成交记录 → 旧日志 / BrainStorm → 作者示例。作者示例只能帮助提问，不能替用户填空。

### 6.2 参数怎样投影到三层

| 你的参数 | 主要修改哪层 | 常见文件 | 验收问题 |
|:---|:---|:---|:---|
| 市场、时区、信息范围、数据预算 | **投研层** | `.env`、Eyes SOP、数据脚本、定时任务 | 能否按需要看到事实，失败时是否降级 |
| 能力圈、策略族、入场、认错、期限、风险预算 | **策略内核层** | Strategy Profile、P 系列、策略 SOP | 同一事实是否按你的裁判得出一致判断 |
| 行为弱点、操作顺序、Human 确认点 | **操盘层** | `rules.json`、计划/决策 SOP、事件契约 | 最容易变形的那一步是否被提前拦住 |
| 阅读偏好、字段顺序、布局、提醒方式 | **Reader / Console 看板** | `config.json`、三个 HTML | 是否更容易看懂和操作，且没有创造第二套策略 |

Reader 是投研内容的阅读界面；交易计划台和买卖决策台是操盘层的可视界面。它们不是独立的第四层，页面字段也不能反过来定义策略。

### 6.3 投研层怎么按自己升级

典型改造顺序：

```text
确定市场与问题
  → 盘点现有 RSS / API / Skill 能否回答
  → 只补一个最缺的数据源
  → 统一输出来源、时间、事实与错误
  → 接入对应 SOP
  → 调整调度频率
  → 观察报告是否真的更有用
```

可修改：RSS 清单、价格来源、问财 Skill、行业专用 API、Watchlist 建档、报告结构和调度节奏。不能修改：用投研脚本偷渡“应该买卖”的个人判断。

### 6.4 策略内核怎么按自己升级

不要从“把 P5 写得更严格”这种抽象要求开始。先指出 Profile 里的具体裁判：

- 哪类事实构成入场依据；
- 什么信号说明原论点失效；
- 价格、基本面、时间、事件发生冲突时谁优先；
- 哪些 P 模块是 `invariant`、`profile` 或 `optional`；
- 哪些作者模块与你无关，应该停用或删除。

Agent 再据此改 P 系列和路由。规则有明确矛盾时走澄清型提案；声称规则更有效时走结果验证型提案，不把个人感悟直接写成稳定纪律。

### 6.5 操盘层与看板怎么按自己升级

Console 是前端样板，可以改字段、顺序、颜色、宽度、提醒和交互。建议从真实使用摩擦出发：

| 使用问题 | 优先改哪里 |
|:---|:---|
| Reader 找不到常看的报告 | `config.json` 的 `reader_pages` |
| 计划字段太多或顺序不合习惯 | `交易计划台.html`，同步检查计划数据契约 |
| 买卖前总漏掉同一个问题 | Profile 红线 → `rules.json` 投影 → `买卖决策台.html` |
| 页面看不清或空间利用差 | HTML / CSS / 本地宽度偏好，不改策略规则 |
| 需要新增生命周期动作 | 先改事件契约与服务端校验，再改页面；历史事件保持 append-only |

所有页面改造都要同时测试 `/reader`、`/plan`、`/sop` 和直接打开 HTML 的预览降级。不要为了视觉统一覆盖用户已经保存的本地布局偏好。

### 6.6 系统扩展的标准落地链

```text
脚本 / API / Skill     提供原子能力与外部手脚
        ↓
SOP                    定义何时调用、输入输出和失败降级
        ↓
Agent 角色             约束谁能读、谁能写、依赖顺序
        ↓
Profile / 状态机       保存当前规则与事实真相
        ↓
Reader / Console       负责阅读、计划、门禁和留痕
        ↓
BrainStorm / 审计      让问题与结果回到下一轮改造
```

新增一个系统级能力时，按这个清单落地：

1. 明确属于哪一层，解决什么真实问题；
2. 说明输入、输出、来源时间和失败降级；
3. 更新 `.env.example` 或配置样例，但不提交真实配置；
4. 接入 SOP 与 Agent 读写边界；
5. 需要显示时再改 Reader / Console；
6. 用正常、空结果、失败、路径越界做最小测试；
7. 更新 README / 进阶指南 / CHANGELOG 中真正受影响的部分。

扩展时记住这句：**脚本提供原子能力，Skill 提供手脚，SOP 负责编排，Agent 定义角色边界，Profile 与状态机保存真相，前端只做投影。**

---

## 七、发布自己的扩展

- 不提交 API Key、账户、持仓或个人运行记录。
- 不把个人参数写成公共默认。
- 参考 Profile 必须标 `disabled_by_default`。
- 提供输入、输出、失败降级和最小测试。
- 明确属于投研层、策略内核层还是操盘层。
- 说明删掉它会影响什么。

---

## 八、常见陷阱与验收清单

### 最容易踩的坑

| # | 陷阱 | 正确做法 |
|:---|:---|:---|
| 1 | Day 0 就想把 Profile 填满 | 写不出来的字段留空；先跑投研层，有真实困惑再配内核 |
| 2 | 让 Agent 从作者案例或「常识」补参数 | 所有数值由你填；缺参数就返回 `data_incomplete` |
| 3 | 把市场洞察当投资雷达 | 洞察不看持仓，雷达才映射到你的 Profile、持仓与关注 |
| 4 | 把「建仓」暗号当成真实成交 | 建仓只建候选；Console 确认成交后才进入真实持仓 |
| 5 | Agent 代写「已执行」「已成交」 | 真实执行证据必须由 Human 操作产生 |
| 6 | 亏了一笔就改规则 | 单笔结果不改内核；结果验证型提案建议至少 10 笔归档证据 |
| 7 | 把脱敏示例当真实数据 | 带 `ymos_sample: true` 的文件不进复盘和真实统计 |
| 8 | 同时维护 Profile 和一套跟它矛盾的 `rules.json` | `rules.json` 只是投影；冲突时以 Human 确认的 Profile 为准 |
| 9 | 个股分析只读一个文件 | 个股文件夹下所有文件都是上下文 |
| 10 | 把所有 SOP 都做成每日定时任务 | 长期启用 4–6 个；先手动跑通再进调度 |

### 验收清单

改造完之后对一遍：

- [ ] 每个新增模块的三层归属明确
- [ ] Profile 里没有系统偷偷补的数值
- [ ] 混合策略写明了裁判冲突时谁优先
- [ ] 驱动失败会明确降级，不静默吞掉
- [ ] 计划与盘中执行是分离的
- [ ] 单笔交易事件只追加，历史改不掉
- [ ] 状态机能恢复最近一次持仓总览
- [ ] Agent 不代写真实执行证据
- [ ] 每个内核修改都有反证、审批和回滚
- [ ] Agent 的统筹、执行与写回真相源没有分裂
- [ ] Reader / Console 只投影规则，没有形成第二套策略
- [ ] 新能力同时更新了配置样例、SOP、权限边界与失败测试
- [ ] 删掉所有可选模块后，最小闭环仍然能跑

---

*YMOS V4 · 进阶指南 · 补投研 → 装策略内核 → 调操盘 → 按证据进化 → 用 Agent 扩展系统*
