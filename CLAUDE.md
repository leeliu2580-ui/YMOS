

```markdown
# CLAUDE.md - 财经自媒体+投研自动化工作区配置

## 🎯 项目核心目标
这是一个财经投研与自媒体自动化工作区。本目录下的所有生成报告、脚本和配置均遵循统一标准，实现半自动化的内容生产与投研分析。

---

## 1. 工作流规范
- **触发方式**：所有任务均通过 Python 脚本调用 `claude --print` 执行，完成单一分析任务后应立即结束会话。
- **文件命名**：生成的报告必须使用英文或拼音命名，避免特殊字符（空格、斜杠等）。
- **报告存储**：生成的报告一律保存在当前工作区（`/mnt/d/7_AI/CC`，即 Windows `D:\7_AI\CC`）。

## 2. 工具与代码风格
- **环境**：运行于 WSL 2 的 Ubuntu 22.04/24.04 环境，Node.js 20 通过 nvm 管理。
- **代码**：Python 脚本必须包含 `#!/usr/bin/env python3` 和 UTF-8 编码声明。
- **依赖**：`requests` 用于 API 调用，直接使用系统 Python 3（无需虚拟环境）。

## 3. 模型与 API 调用
- **模型**：使用 MiniMax M2.7（`MiniMax-M2.7-highspeed`），通过 `ANTHROPIC_BASE_URL` 和 `ANTHROPIC_API_KEY` 环境变量配置。
- **输出**：报告必须包含标题和层级分明的结构化内容，输出简洁明确，不添加多余解释。

## 4. Prompt 工程规范
- **模板**：生成报告需遵循固定模板：
  - **标题**：【选题名称】
  - **核心观点**：
  - **逻辑支撑**：1. 2. 3.
  - **风险提示**：
- **迭代**：测试 Prompt 时，请明确对比新旧版本的差异，并记录修改原因（保存于 `prompts/CHANGELOG.md`）。

## 5. Git 与版本控制
- **提交规范**：遵循 Conventional Commits 格式，如 `feat: 更新了 Prompt 模板`、`fix: 修复报告生成脚本路径`。
- **忽略文件**：将通过脚本自动生成的 `report_*.md` 文件以及 `CLAUDE.local.md` 加入 `.gitignore`。

---

## ⚠️ 能力边界（重要）
- **你没有实时行情、财报、宏观经济数据的直接访问权限**。
- **所有涉及实时数据的内容必须通过 Tavily 搜索获取**，或由用户提供数据来源。
- **如果无法获取真实数据，请明确告知用户“数据未获取到”，不要编造数字或日期。**

---

## ✅ 当前可用能力（已验证）
| 能力 | 触发方式 | 说明 |
|------|----------|------|
| Tavily 搜索 | 自然语言“搜索一下...” | 可搜索财经新闻、公司动态、市场热点 |
| 飞书文档/云盘/权限/知识库 | 自然语言“在飞书文档中...”、“上传到飞书云盘” | 读写操作 |
| 天气查询 | “今天天气” | 基于 wttr.in |
| 视频帧提取 | “提取视频关键帧” | 需提供视频路径 |
| 任务编排 | “创建一个任务流...” | TaskFlow 基础功能 |
| 安全审计 | “检查系统安全” | healthcheck 技能 |
| 技能管理 | “安装/搜索/更新技能” | clawhub + skill-creator |
| 文件读写 | 当前工作目录 `/mnt/d/7_AI/CC` | 生成报告、保存脚本 |
| Python 脚本执行 | 运行 `~/generate_report.py` | 已有报告生成脚本 |

---

## 🧭 核心原则（人机协作）
| 原则 | 执行标准 |
|------|----------|
| 确定性优先 | 数据抓取、格式转换等用代码/规则 |
| AI 赋能 | AI 承担摘要、归纳、生成、多文档推理 |
| 半自动化 | 外发内容必须人工审校 |
| 可观测性 | 记录操作日志 |
| 成本可控 | 优先本地化，控制 API 调用 |

---

## 📝 报告输出格式（可复用模板）
当生成投研报告时，请使用以下结构（若无可信数据，请省略表格，输出分析框架）：

```markdown
# 【选题标题】

**核心观点**：
【一句话结论】

**逻辑支撑**：
1. 【依据1】
2. 【依据2】
3. 【依据3】

**风险提示**：
- 【风险1】
- 【风险2】

> 声明：以上内容仅供参考，不构成投资建议。数据来源：Tavily搜索 / 用户提供。
```

## ✍️ 自媒体选题格式
```markdown
# 自媒体选题

## 选题一：【标题】
- **核心角度**：...
- **目标受众**：...
- **建议体裁**：公众号/短视频/图文
```

---

## 🔐 安全与合规
- 所有投资建议必须标注 **“仅供参考，不构成投资建议”**
- 必须包含风险提示章节
- 禁止直接推荐买入/卖出
- 禁止编造数据、财报、行情
- 敏感话题（政治、监管）需谨慎处理

---

## 📂 工作目录与 YMOS 暗号执行方法
- **工作目录**：`/mnt/d/7_AI/CC`（Claude Code 启动后自动位于此目录）
- **YMOS 暗号**：Claude Code 无法直接触发 YMOS，但可以生成执行命令供用户在 WSL 终端运行。例如：
  - 执行“跑一下投资雷达”：
    ```bash
    cd `/path/to/YMOS` && python3 -c "from eyes.radar import run; run()"
    ```
  - 执行“市场洞察”：
    ```bash
    cd `/path/to/YMOS` && python3 -c "from eyes.insight import run; run()"
    ```
  请根据用户实际的 YMOS 路径替换 `/path/to/YMOS`。

---

## 📅 PDCA 闭环（用户侧）
- **每日**：用户运行 `python3 ~/generate_report.py <选题>` 生成报告
- **每周**：用户收集反馈，优化 Prompt
- **每月**：迭代系统能力

---

*配置版本：v3.0（完整工程规范版）*  
*最后更新：2026-05-03*  
*适用于 WSL 2 + Claude Code + MiniMax M2.7*
```

---

## 使用说明
1. 将以上内容完整保存为 `/mnt/d/7_AI/CC/CLAUDE.md`（即你的工作目录）。
2. 如果 Claude Code 已启动，输入 `/reset` 重新加载配置。
3. 该文件融合了你的工程规范（工作流、工具、API、Prompt、Git）和 Claude Code 的能力边界、可用技能清单，确保 AI 行为既符合你的习惯，又不超出真实能力。

这份配置可以直接用于你当前的财经自动化系统。