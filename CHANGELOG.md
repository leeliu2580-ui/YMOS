# Changelog

## V4.1 — 2026-08-04（买卖决策台：分批建仓与平仓结转）

两个来自实盘使用的账本口径修复。都属于「系统记的账和真实账户对不上」这一类问题 —— 不改结构红线，只让数字变回真的。

### 分批建仓：新增「建仓中」状态

- 一份 100% 的建仓计划，现实里往往分成几笔买。旧版第一笔成交就直接判定「建仓完成」，剩下没执行的计划从系统里消失 —— 而真正需要被追踪的恰恰是「我的计划还有多少没执行」。
- 交易状态从两态扩为四态：`计划中` → `建仓中` ⇄ `持仓中` → `已平仓`。`fill` 事件可重复追加，每次只送**本次这一笔**的增量；累计股数、累计投入与加权平均成本一律由服务端按 front matter 既有事实推进，界面算错了也污染不了账本。
- 收口由人显式决定：勾选「这笔建仓已完成」才转 `持仓中`；也支持「只收口、不再追加」的 `finalizeOnly` 事件。
- 「加仓」重新定位为**改建仓计划总额**，不改持仓事实，执行后状态回到 `建仓中` —— 实际成交仍然回「确认建仓」录入。整套系统里只有一个地方能写持仓事实。
- `建仓中` 的仓位是真实的：账户敞口按实际成交计入，止损、减仓与平仓都不再要求「先建满」。没建满不是继续扛着的理由。
- 首页新增「建仓中（分批未建满）」分区，显示建仓进度、已建金额与还差多少；「待建计划」= 计划中的全额 + 建仓中未建满的那一截。
- front matter 新增 `建仓计划` / `建仓更新日`；`建仓完成日` 改为收口日期。

### 平仓结转：资金基数不再是死数

- 旧版平仓后账户资金基数永远停在开户那天的数字，已实现盈亏也常常因为卖出成交价没填而算不出来 —— 结果是「平仓了，钱却没进账」。
- 平仓归档成功后，该笔的累计已实现盈亏（含此前所有减仓）自动并入所属账户的 `capital`，写入 `changes` 与新增的 `settlements` 结转台账；账户参数页与状态机文件都能查到「资金基数 从多少 → 变成多少」。
- 账户净值口径改为 `资金基数 + 未结转已实现盈亏 + 浮动盈亏`：已结转的交易不再重复计入。
- 卖出成交价从选填改为**必填**，并在卖出页实时显示本次已实现盈亏；平仓表单的「实际盈亏」由系统结算，不再是自由文本。
- 资金基数是单笔上限与仓位占比的分母。分母停在过去，风控约束的就是一个已经过期的自己。

### 建仓计划可双向调整，零成交计划可作废

成交之后建仓计划原本是单向棘轮：只能靠加仓调大，想缩减只能把文件扔掉重来——而重来会洗掉整条认知链。

- 「加仓计划」正名并泛化为**「变更建仓计划」**：直接填变更后的总额。调大 = 加仓，走压力测试门；调小 = 缩减敞口，跟止损同类不设门，但**必须写理由**。
- 硬下限是**累计已建仓金额**，服务端按事件流校验。想降到已建仓以下，要表达的是「这笔到此为止」，对应动作是建仓收口——缩到刚好等于已建仓时系统会自动收口。
- 新增事件 `trim`（缩减建仓计划），与 `add` 共用同一套计划口径；`decisionPlan` / `effectivePlan` / `selectTrade` 三处同步。
- 新增 `void` 事件与 `/api/trade/void`：**只对零成交的计划**开放，必须写原因，状态改 `已作废` 并移进 `已作废/YYYY/`。有过真实成交的一律走平仓——有仓位就必须有出场记录。
- 首页计划表加「🗑 作废」入口，并新增「已作废的计划」折叠区；作废后不再占用「待建计划」与「剩余可开」。
- `/api/trade/list` 新增 `voided` 桶。

改计划与作废回答的是不同问题：**「我改主意了」** 走事件留痕，**「这条记录本就不该存在」** 才走归档。用作废去做前者，会让「主动缩减」和「从没打算买那么多」在复盘时变得无法区分。

### 其他

- `/api/trade/prices`、`sell`、`close`、`adjust`、`add` 的状态机同步纳入 `建仓中`。
- 修正 `fill` 金额校验：改用精确到分的比对，非整数成交金额（如 10.5 × 3）不再被误判为 `fill amount mismatch`。
- `Console/tests/test_trade_api.py` 覆盖分批建仓、收口、加仓改计划、建仓中减仓/平仓与结转台账。

## V4.0 — 2026-07-27（文档与抽象补全：2026-08-02）

V4 保留 V3 的市场洞察、投资雷达、标的管理、P 系列和 Markdown 状态机主链，在此基础上补齐三层结构：

```text
投研层（看市场、做研究） → 策略内核层（按自己的逻辑判断） → 操盘层（计划、门禁、执行、留痕）
```

> 2026-08-01 至 08-02 完成的策略抽象、交易生命周期和反馈闭环仍并入 V4，不另立小版本。大多数用户的真实迁移路径是 **V3 → V4**。

### 投研层

- V3 的市场洞察、投资雷达、持仓 / Watchlist 管理和初始调研继续作为新用户第一入口。
- 新增可替换的 Skills 与数据适配层；问财作为可选 Beta，未安装不阻塞主链。
- Reader 并入 Console，一个端口聚合报告；新用户可以只使用 Reader，不必先启用操盘工作台。
- 市场洞察、投资雷达和策略分析各新增一份带 `ymos_sample: true` 的脱敏合成产物，首次打开 Reader 即可看到三层如何衔接；示例不得进入动作路由、P11 或真实统计。
- 恢复 `Agents/SCHEDULES_REFERENCE.md`：保留真实时序依赖、踩坑约束和停用原则。定时任务是日常投研运行机制，具体任务与频率由用户选择。
- RSS 恢复系统 TLS 证书校验，并为每个来源保存抓取时间、状态、条数与失败原因；模板示例行不再触发行情或个股新闻请求。
- Crypto 价格路由兼容 `BTC`、`BTC-USD` 与 Finnhub 交易所格式，统一在数据源调用前归一化。

### 策略内核层

- 新增可校验的 Strategy Profile schema、空白模板和配置说明，把散落在偏好、P 系列、SOP 与脑中的规则收拢成统一接口。
- `Brain/策略配置/当前策略_Profile.md` 成为唯一运行 Profile；`draft` 不阻塞事实投研，但动作级请求返回 `kernel_not_ready`。`active` 必须有 Human 批准、非空 scope、风险字段与模块清单。
- 首次策略配置改为“投研先用 + AI 快速入职”：不配送默认激活的通用策略；Agent 用六组人类问题生成安全草稿，草案保存与正式激活分两次 Human 确认，`kernel_not_ready` 作为可恢复配置状态呈现。
- 所有动作路由先检查 `modules.enabled / disabled / replacements`；缺参数返回 `data_incomplete`，不得从作者案例或模型常识补默认值。
- P1–P18 完整开放作者实际使用的方法，并标注 `invariant` / `profile` / `optional`；具体阈值、比例、权重、持有周期和收益区间由用户定义。
- 六类策略结构只作为诊断坐标，不替用户选流派；混合策略必须写清裁判冲突时谁优先。
- `ymos-diagnosis` 升级为通用结构诊断，并通过 Adapter 映射到 Profile 与 Console。
- BrainStorm 成为重要的策略梳理与进化语料入口；旧投资日志和复盘记录可作为上下文，但不自动升级为生效规则。
- 新增内核入职、平仓复盘归档、周期审计，以及包含证伪、审批和回滚字段的变更提案。
- 周期审计由定时任务驱动；样本少于 10 笔时只登记观察，不因少数交易结果修改规则。

### 操盘层

- 新增 Console：Reader、交易计划台和买卖决策台；前端与门禁是作者打样，用户可按自己的 Profile 和习惯修改。
- Reader 默认递归发现已登记目录中的新月份和任意层级子目录；新增 `reader_custom_paths`，用户登记一条相对或绝对路径即可自动生成“自定义工作区”，无需复制整份页面配置。
- 买入逻辑建档、建仓准备、成交、减仓、退出规则调整和平仓形成 append-only 完整生命周期；V4 的加仓入口明确为计划/压力测试，不伪造成交或改写持仓事实。
- 账户支持分币种、资金期限、资金变更流水、最近行情、成本、敞口和 Agent 可读持仓快照。
- 真实成交、已执行计划与规则批准必须由 Human 产生；Agent 不得伪造闭环。
- Console 服务端强制校验身份、合法状态迁移、成交金额、部分卖出余量与全部平仓归零；前端门禁拦截和成功动作分别写入结构化审计。

### 反馈闭环与开源边界

- 两类进化原料并行：BrainStorm / 旧日志用于梳理与发现矛盾，计划 / 审计 / 已平仓单笔用于验证判断和执行。
- 打通「研究与思考 → 策略判断 → 操盘证据 → 归档 → 跨笔聚合 → 变更提案 → Human 审批 → 策略内核更新」。
- P11 改读 V4 已平仓事件、交易计划和决策审计，不再写 V3 备忘录或状态机；复盘机械检出风险扩大、违反 Profile 加仓规则和门禁绕过。
- 灰度或 DEMO 文件必须标记并从真实内核审计样本中排除，不能用合成盈亏生成规则提案。
- 通用主闭环不启用作者参数；P17/P18、What's Hot 等真实扩展案例标注 `disabled_by_default`。
- 本地正式模式坚持 Markdown-first；浏览器缓存不作为持仓与交易真相源，公开仓库不包含真实账户、持仓、个人参数或敏感配置。

## V3.1 — 2026-03-28

- 首次加入独立策略诊断模块。

## V3.0 — 2026-03-19

- 建立 Eyes、Brain 与持仓状态协作链。
- 提供 P 系列研究提示词、价格路由、Markdown 状态机与 Human 最终确认。
